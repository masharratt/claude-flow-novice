# WS04 brief: careful-guard deny funnel

Run dir: `planning/fleet-jev-batch2/`. Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 4** (all 3 rows) of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first. PREREQ: `jev-shadow-lib.sh` (WS01); if absent, heartbeat blocked.

## Claims (only these files)

- `tests/test-jev-careful-guard.sh` (new)
- `.claude/hooks/cfn-careful-guard.sh` (refactor: 8 deny sites -> DENY_RULE var + ONE shared exit 2 + funnel)
- `.claude/skills/cfn-careful/lib/jev-deny-check.sh` (new)

## Method (TDD)

Test RED first, then lib script, then hook refactor, GREEN.

## Constraints

- Behavior-identical hook: same 8 rules, same order, same rc 2. Only structure changes: each deny branch sets `DENY_RULE=<name>` and falls through to one shared `exit 2`; the funnel call (`timeout 2 $HOME/.claude/skills/cfn-careful/lib/jev-deny-check.sh --rule "$DENY_RULE" --cmd "$cmd" || true`) sits immediately before it. Whitelist and allow paths NEVER reach the funnel.
- Test invokes the hook as a SUBPROCESS (heredoc stdin, assert rc) — never source it (it calls exit).
- Allow path must show ZERO curl invocations in the stub log; curl-timeout (stub rc 28) -> skip line, rc still 2.
- NO em dashes. `bash -n` clean. Stub curl idiom per `tests/test-jev-vote-triage.sh:12-57`.
- You may run ONLY: your new test, `bash -n`.
- Per file: pre/post edit hooks with `--agent-id WS04`.

## Fleet protocol

- `fleet heartbeat WS04 --files <list>` on edit-set change.
- Commit: `fleet commit WS04 -m "feat(jev): careful-guard deny shadow funnel" --force-with-note`.
- Never push. Done: `fleet heartbeat WS04 done: ...`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
