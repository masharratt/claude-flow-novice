# WS03 brief: tmux outcome classifier

Run dir: `planning/fleet-jev-batch2/`. Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 3** (all 3 rows) of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first. PREREQ: `jev-shadow-lib.sh` must exist (WS01); if absent, heartbeat `blocked: waiting for jev-shadow-lib.sh`.

## Claims (only these files)

- `tests/test-jev-tmux-outcome.sh` (new)
- `.claude/skills/cfn-tmux-agents/lib/jev-outcome.sh` (new)
- `.claude/skills/cfn-tmux-agents/execute.sh` (one insertion in the regex-miss branch ~:218-223)

## Method (TDD)

Test RED first, then the new script GREEN, then the execute.sh insertion, then `bash .claude/skills/cfn-tmux-agents/tests/test-agents.sh` must stay green (it exercises the miss branch at its line ~90).

## Constraints

- Choice of 6 verdicts exactly as plan row 2 names them; log `{type:"outcome",name,engine,verdict,confidence,tail_first<=120}` to `jev-tmux-outcome.jsonl`; exit 0 always.
- The execute.sh call: `timeout 5 $HOME/.claude/skills/cfn-tmux-agents/lib/jev-outcome.sh ... || true`; dump + `exit 68` unchanged.
- NO em dashes. `set -euo pipefail` (new script), `bash -n` clean. Stub curl idiom per `tests/test-jev-vote-triage.sh:12-57`.
- You may run ONLY: your new test, `.claude/skills/cfn-tmux-agents/tests/test-agents.sh`, `bash -n`.
- Per file: pre/post edit hooks with `--agent-id WS03`.

## Fleet protocol

- `fleet heartbeat WS03 --files <list>` on edit-set change.
- Commit: `fleet commit WS03 -m "feat(jev): tmux outcome shadow classifier" --force-with-note`.
- Never push. Done: `fleet heartbeat WS03 done: ...`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
