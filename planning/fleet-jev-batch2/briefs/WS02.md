# WS02 brief: retry-context triage + shrink reason

Run dir: `planning/fleet-jev-batch2/`. Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 2** (all 6 rows) of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first. PREREQ: `.claude/cfn-scripts/jev-shadow-lib.sh` must exist (lane WS01 lands it); if absent, heartbeat `blocked: waiting for jev-shadow-lib.sh` and wait.

## Claims (only these files)

- `tests/test-jev-retry-context.sh` (new)
- `tests/test-jev-loop-shadow.sh` (new)
- `.claude/skills/cfn-loop-orchestration-v2/lib/task-mode/jev-retry-context.sh` (new)
- `.claude/skills/cfn-loop-orchestration-v2/lib/task-mode/jev-shrink-reason.sh` (new)
- `.claude/skills/cfn-loop-orchestration-v2/lib/task-mode/iteration-context.md` (one SHADOW insertion near :11)
- `.claude/commands/cfn-loop-task.md` (two one-line SHADOW pointers: ~:666 retry-context output note; exit-3 cell ~:477 + echo ~:705 shrink-reason)

## Method (TDD)

Rows 1-2 and 4: tests first RED, then scripts GREEN. Rows 5-6 wiring: wiring greps live inside `tests/test-jev-loop-shadow.sh`, red until the two docs carry the `$HOME/...` invocations.

## Constraints

- 8-bucket Choice menu exactly as the plan names it; units capped 60; chunk 25; reduced file at `/tmp/test-context-${RUN_ID}.md`; stats JSONL line shape per plan row 2.
- Noul question for shrink per plan row 3; no stdout.
- Source `$HOME/.claude/cfn-scripts/jev-shadow-lib.sh`; logs at `${CFN_DATA_DIR:-$HOME/.claude/cfn-data}/jev-*.jsonl`.
- NO em dashes. `set -euo pipefail`, `bash -n` clean. Stub curl idiom per `tests/test-jev-vote-triage.sh:12-57`.
- You may run ONLY: your two new tests + `bash -n`. No other suites.
- Per file: pre/post edit hooks with `--agent-id WS02`.

## Fleet protocol

- `fleet heartbeat WS02 --files <list>` on edit-set change.
- Commit: `fleet commit WS02 -m "feat(jev): retry-context triage and shrink-reason shadow" --force-with-note`.
- Never push. Done: `fleet heartbeat WS02 done: ...`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
