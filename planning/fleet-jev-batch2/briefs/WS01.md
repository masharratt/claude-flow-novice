# WS01 brief: shared Jev shadow lib + summarizer

Run dir: `planning/fleet-jev-batch2/`. Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 1** (all 6 rows) of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first.

## Claims (only these files)

- `tests/test-jev-shadow-lib.sh` (new)
- `.claude/cfn-scripts/jev-shadow-lib.sh` (new)
- `.claude/cfn-scripts/jev-shadow-summary.sh` (new)
- `.claude/skills/cfn-vote-implement/jev-triage.sh` (refactor: source the lib)
- `.claude/skills/cfn-vote-implement/jev-shadow-record.sh` (same refactor)
- `.claude/cfn-scripts/jev-systemone.sh` (add `--max-time 4` to curl; add the matching timeout case to `tests/test-jev-systemone.sh` ONLY if that file is within scope: it is NOT claimed, so instead verify the existing suite passes with the flag; if a new test case is required, heartbeat blocked and report)

## Method (TDD)

1. `tests/test-jev-shadow-lib.sh` first, run RED (lib absent), then implement lib + summarizer, GREEN.
2. Refactor the two batch-1 scripts to source `$HOME/.claude/cfn-scripts/jev-shadow-lib.sh`; `bash tests/test-jev-vote-triage.sh` must stay green (48 cases).
3. `bash tests/test-jev-systemone.sh` green after the `--max-time 4` change.

## Constraints

- `#!/usr/bin/env bash`, `set -euo pipefail` (scripts), `bash -n` clean, NO em dashes anywhere.
- Copy the flock `append_line()` semantics exactly from `.claude/skills/cfn-vote-implement/jev-triage.sh:56-61`.
- Stub curl idiom per `tests/test-jev-vote-triage.sh:12-57` (`JEV_CURL_LOG`, `JEV_CURL_RC`, `JEV_CURL_BODY_FILE`, `CFN_DATA_DIR` temp).
- You may run ONLY: your new test, `bash tests/test-jev-vote-triage.sh`, `bash tests/test-jev-systemone.sh`, `bash -n`.
- Per file: `~/.claude/hooks/cfn-invoke-pre-edit.sh <file> --agent-id WS01` before, post-edit after.

## Fleet protocol

- `fleet heartbeat WS01 --files <list>` on edit-set change.
- Commit: `fleet commit WS01 -m "feat(jev): shared shadow lib, summarizer, bounded curl" --force-with-note`.
- Never push. Done: `fleet heartbeat WS01 done: ...`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
