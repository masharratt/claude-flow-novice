# WS06 brief: alpha gap bucketing

Run dir: `planning/fleet-jev-batch2/`. Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 6** (all 3 rows) of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first. PREREQ: `jev-shadow-lib.sh` (WS01); if absent, heartbeat blocked.

## Claims (only these files)

- `tests/test-jev-gap-bucket.sh` (new)
- `.claude/skills/cfn-alpha-launch/jev-gap-bucket.sh` (new)
- `.claude/skills/cfn-alpha-launch/execute.sh` (one insertion in the `--mode manifest` code path, execute.sh:397+, after the fix-list existence check)

## Method (TDD)

Test RED first, then script GREEN, then execute.sh insertion; `bash -n .claude/skills/cfn-alpha-launch/execute.sh` clean.

## Constraints

- IMPORTANT: execute.sh lines ~360-392 are heredoc INSTRUCTION TEXT, not shell code. Wire ONLY inside the `--mode manifest` code path after the fix-list existence check. Never touch the heredoc.
- Choice {critical,high,medium} per numbered fix-list item; log `{type:"gap",fixlist_mtime,item_no,section_now,jev_bucket,confidence}`; fix-list.md byte-identical; rerun same mtime appends 0.
- NO em dashes. `set -euo pipefail` (new script), `bash -n` clean. Stub curl idiom per `tests/test-jev-vote-triage.sh:12-57`.
- You may run ONLY: your new test + `bash -n`.
- Per file: pre/post edit hooks with `--agent-id WS06`.

## Fleet protocol

- `fleet heartbeat WS06 --files <list>` on edit-set change.
- Commit: `fleet commit WS06 -m "feat(jev): alpha gap bucket shadow classifier" --force-with-note`.
- Never push. Done: `fleet heartbeat WS06 done: ...`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
