# WS07 brief: night-mode risk scoring

Run dir: `planning/fleet-jev-batch2/`. Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 7** (all 3 rows) of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first. PREREQ: `jev-shadow-lib.sh` (WS01); if absent, heartbeat blocked.

## Claims (only these files)

- `tests/test-jev-night-risk.sh` (new)
- `.claude/skills/cfn-night-mode/jev-night-risk.sh` (new)
- `.claude/skills/cfn-night-mode/night-mode.sh` (one per-item insertion in the render loop ~:167-181)

## Method (TDD)

Test RED first, then script GREEN, then night-mode.sh insertion. Then `env -u TYPESAFE_API_KEY bash tests/test-night-mode.sh` must stay green (skip path: no paid calls, report byte-identical).

## Constraints

- Insertion shape exactly per plan row 3: `[ -x $HOME/.claude/skills/cfn-night-mode/jev-night-risk.sh ] && timeout 5 ... || true`; sections and ordering unchanged.
- Score 0-2 "needs human attention now"; log `{type:"risk",decision_id,slug,project,score,confidence}` to `jev-night-risk.jsonl`.
- NO em dashes. `set -euo pipefail` (new script), `bash -n` clean. Stub curl idiom per `tests/test-jev-vote-triage.sh:12-57`.
- You may run ONLY: your new test, `env -u TYPESAFE_API_KEY bash tests/test-night-mode.sh`, `bash -n`.
- Per file: pre/post edit hooks with `--agent-id WS07`.

## Fleet protocol

- `fleet heartbeat WS07 --files <list>` on edit-set change.
- Commit: `fleet commit WS07 -m "feat(jev): night-mode risk shadow scorer" --force-with-note`.
- Never push. Done: `fleet heartbeat WS07 done: ...`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
