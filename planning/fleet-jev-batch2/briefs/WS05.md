# WS05 brief: tech-debt rot-risk scoring

Run dir: `planning/fleet-jev-batch2/`. Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 5** (all 3 rows) of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first. PREREQ: `jev-shadow-lib.sh` (WS01); if absent, heartbeat blocked.

## Claims (only these files)

- `tests/test-jev-rot-risk.sh` (new)
- `.claude/skills/cfn-tech-debt/jev-rot-risk.sh` (new)
- `.claude/skills/cfn-tech-debt/harvest.sh` (one fire-and-forget call after ledger JSON write ~:237)

## Method (TDD)

Test RED first, then script GREEN, then harvest.sh call, then `bash .claude/skills/cfn-tech-debt/tests/test-harvest.sh` stays green.

## Constraints

- Score 0-2 per `has_trigger==false` row; ledger JSON byte-identical after run (assert in test); log line shape per plan row 2.
- harvest.sh insertion: `timeout 5 $HOME/.claude/skills/cfn-tech-debt/jev-rot-risk.sh --ledger "$LEDGER_JSON" || true`.
- NO em dashes. `set -euo pipefail` (new script), `bash -n` clean. Stub curl idiom per `tests/test-jev-vote-triage.sh:12-57`.
- You may run ONLY: your new test, `.claude/skills/cfn-tech-debt/tests/test-harvest.sh`, `bash -n`.
- Per file: pre/post edit hooks with `--agent-id WS05`.

## Fleet protocol

- `fleet heartbeat WS05 --files <list>` on edit-set change.
- Commit: `fleet commit WS05 -m "feat(jev): tech-debt rot-risk shadow scorer" --force-with-note`.
- Never push. Done: `fleet heartbeat WS05 done: ...`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
