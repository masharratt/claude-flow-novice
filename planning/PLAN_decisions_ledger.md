# Implementation Plan: decisions-ledger writer for cfn-workbench

**Date:** 2026-07-28
**Mode:** standard (beta tier)
**Spec:** planning/SPEC_decisions_ledger.md
**Decisions:** planning/DECISIONS_decisions_ledger.md (D-1..D-9; D-1, D-7, D-8, D-9 BLOCKING, all user-approved 2026-07-28)
**Pseudo:** planning/PSEUDO_decisions_ledger.md
**Arch:** planning/ARCH_decisions_ledger.md
**Ops:** planning/OPS_decisions_ledger.md
**Test:** planning/TEST_decisions_ledger.md

Closes the gap where the workbench renderer reads `planning/.VERIFY_<slug>.decisions.json`
(`.claude/skills/cfn-workbench/lib/section-decisions.sh:14,38-51`) but nothing writes it.
Builds a new `cfn-decisions` skill that OWNS the per-run JSON and DELEGATES the SQLite sync
to the LOCKED `decision-log/record.sh` sink (composition, not duplication). Wires the
writer into 4 coordinator hook sites with D-8 failure isolation.

## Task Analysis

- **Complexity:** Standard (4 components per ARCH §1; 2 NEW: writer + hook wrapper; 2 LOCKED consumers: sink + renderer)
- **Estimated Files:** 12 new (1 entrypoint + 5 lib modules + 1 SKILL.md + 1 state-machine doc + 4+ test files under tests/) + 4 modified coordinator files
- **Estimated LOC:** 350-450 (writer libs ~250, tests ~150, prose edits ~50 lines across 4 sites)
- **Mode:** standard (beta tier)
- **Build flags:** frontend=no, db=no, pii=no, unknowns=no (SPEC §8)
- **Actors (§1a):** ABSENT (frontend=no AND db=no; single implicit caller: the loop coordinator + megaplan orchestrator)
- **Interaction Intent (§1b):** ABSENT (frontend=no)
- **Resolved forks:** D-1 (new skill cfn-decisions/), D-7 (JSON-first/SQLite-best-effort, never roll back), D-8 (isolate hook failures at ALL 4 sites), D-9 (writer replaces cfn-decide as canonical at megaplan:215). All user-approved 2026-07-28.

**Step 7 promotions (user-approved 2026-07-28):** Q1 add `SINK_TIMEOUT_SECONDS=30` timeout wrapper at the sink-call boundary inside the writer (covers all 4 coordinator hook sites; DRY one entry point; new step A.9 + AC-65/AC-66). Q3 pin `section-decisions.sh` sha256 `97ec8ef0f0bccebc8ff0d0024cf85a59b9cc9fe21d23a8aa8f725264417f2fb6` in WIRE-4 (replaces the git-diff fallback; AC-64 check+pass updated). Q2 accept single-writer assumption as a documented limitation (F4 note at line 31, carried into MEGAPLAN as accepted-as-documented).

**Scope (F2):** `cfn-megaplan/SKILL.md:300` is a SEPARATE pre-existing recording path (Step 7 accepted-defaults via `cfn-decide`), NOT edited by this plan. D-9 scopes to `:215` only. The `:300` inconsistency (two recording paths documented in the same file post-edit) is pre-existing and out of scope.

**Limitation (F4):** Single-writer assumption. Coordinator runs are serial within a task; concurrent distinct-id writes (two simultaneous runs on the same slug) can lose one decision via last-write-wins on the atomic `mv`. Conservative park: document now; flag for future file-locking if cross-run concurrency becomes real. No locking code added (out of scope; advisory finding on a terminal-phase AC).

## Agent Configuration

### Loop 3 (Implementation)

Lane A (writer skill, files owned exclusively by this lane):
- backend-developer (writer bash+jq, OP-W0..OP-W4)
- tester (bash+jq test files, plain bash runner per TEST §framework)

Lane B (coordinator hooks, SERIAL AFTER Lane A per OPS §3):
- backend-developer (4 prose+snippet edits in cfn-loop-task.md + cfn-megaplan/SKILL.md)

### Loop 2 (Validation)

- code-reviewer (DRY, structural)
- security-specialist (FR-9 leakage audit per ARCH §10.4; SQL/XSS injection EC-13/14)
- test-validation-agent (64 ACs, AC-2 cardinality + AC-4 atomicity load-bearing)
- strategic-alignment-reviewer (WIRE-1..WIRE-4 composition-root grep; D-9 SITE 4 substitution behavior-preserving)

### Product Owner

- product-owner (PROCEED/ITERATE/ABORT decision at the Loop 3 exit gate)

## Assembly Rule

When megaplan artifacts exist (SPEC, PSEUDO, ARCH, OPS, TEST in planning/), this plan is an ASSEMBLY document: the step table below + the inherited TEST tables + the OPS integration rows + the risk register. The Implementation Steps table replaces freehand Red/Green/Refactor phases; the TDD Sequence (section after Implementation Steps) binds every step to a red-first test per TEST Phase 6.

## Lanes (Phases)

Two lanes with a strict A→B edge (record.sh is the produced identifier Lane B's hooks consume):

| Lane | Scope | Files owned (exclusive) | Cap | Edge |
|---|---|---|---|---|
| A | Writer skill (producer) | `.claude/skills/cfn-decisions/{SKILL.md, record.sh, lib/arg-parse.sh, lib/jq-build.sh, lib/upsert.sh, lib/sink-delegate.sh, lib/help.sh, lib/state-machine-doc.md}`, `.claude/skills/cfn-decisions/tests/*.sh` | 8 | produces the writer identifier Lane B consumes |
| B | Coordinator hooks (consumer) | `.claude/commands/cfn-loop-task.md` (3 sites), `.claude/skills/cfn-megaplan/SKILL.md` (1 site) | 4 | consumes Lane A's `record.sh`; SERIAL AFTER Lane A per OPS §3 |

**A→B dependency:** Lane B step B.1 consumes `.claude/skills/cfn-decisions/record.sh` (the writer identifier Produced by Lane A step A.2). Lane A MUST be green (`bash .claude/skills/cfn-decisions/tests/run-all.sh` exits 0) before Lane B starts. Per OPS §3 Stage 1 promote gate.

LOCKED files (this plan MUST NOT modify; call/delegate only):
- `.claude/skills/cfn-workbench/lib/section-decisions.sh` (renderer)
- `.claude/skills/decision-log/record.sh` + `schema.sql` (SQLite sink)
- `.claude/skills/cfn-megaplan/bars/bless-verify.sh` (append-only event ledger, separate lifecycle)

## Phase 2: Green (Implementation Steps)

Each step binds to a failing test written FIRST (TDD Sequence section below). One step = one file where possible. Never more than 3 files per step. Every step names the file path + signature/behavior + control type + error path (Bar B structural requirement).

### Lane A: Writer Skill (producer)

| # | File (full path) | Change (exact: function name, typed signature, or config key) | Produces | Consumes | Failing test (TEST Phase 6) | Verify command (exits 0/1) | Done predicate |
|---|---|---|---|---|---|---|---|
| A.1 | `.claude/skills/cfn-decisions/SKILL.md` | create skill manifest: name, description, invocation contract; embed writer CLI table from ARCH §2.1 (12 flags + FR-10 defaults + reject-flag list `--delete/--remove/--purge/--supersede`); cross-reference LOCKED sink (`decision-log/record.sh`) and LOCKED renderer (`section-decisions.sh`); zero em dashes (NFR-5) | `.claude/skills/cfn-decisions/SKILL.md` | - | (no test; skill manifest; the writer entrypoint is what tests exercise) | `grep -nP '\x{2014}' .claude/skills/cfn-decisions/SKILL.md` returns zero matches AND `grep -c 'cfn-decisions/record.sh' .claude/skills/cfn-decisions/SKILL.md` >= 1 | file exists; flags table documents all 12 flags; no em dashes |
| A.2 | `.claude/skills/cfn-decisions/record.sh` | OP-W0 entrypoint: `main(argv) -> exit(0\|1\|2\|3\|4\|5\|7\|8)`. Sources `lib/arg-parse.sh`, `lib/jq-build.sh`, `lib/upsert.sh`, `lib/sink-delegate.sh`. Dispatches op results to exit codes per ARCH §2.2 enum. On success: `printf '%s %s\n' "$DEC_ID" "$STATUS"` to stdout (FR-9: id+status only). Trap on EXIT/INT/TERM removes any lingering `$TMP` (FR-4 invariant). Startup check: `command -v jq >/dev/null 2>&1 \|\| exit 2` (mirrors `bless-verify.sh:60`). NEVER emits exit 6 (D-7 reservation). | `.claude/skills/cfn-decisions/record.sh` | `.claude/skills/cfn-decisions/lib/arg-parse.sh:parse_and_validate_args`, `.claude/skills/cfn-decisions/lib/jq-build.sh:build_decision_object`, `.claude/skills/cfn-decisions/lib/upsert.sh:upsert_by_key_atomic`, `.claude/skills/cfn-decisions/lib/sink-delegate.sh:delegate_to_record_sh` | TDD steps 4, 5, 6 (AC-16 defaults, AC-17 actor enum, AC-3 refuse-missing; all unit reds) | `bash .claude/skills/cfn-decisions/tests/95-defaults.sh 2>&1 \| tee "$OUT"` AND `bash .claude/skills/cfn-decisions/tests/96-actor-enum.sh 2>&1 \| tee "$OUT"` AND `bash .claude/skills/cfn-decisions/tests/30-refuse-missing.sh 2>&1 \| tee "$OUT"` | all 3 tests green; record.sh executable (`-x`); never calls `exit 6` (static grep `grep -nE 'exit 6\b' record.sh` returns zero) |
| A.3 | `.claude/skills/cfn-decisions/lib/arg-parse.sh` | OP-W1 `parse_and_validate_args(argv) -> ParsedArgs\|Exit(2)` + OP-W1b `refuse_on_missing_or_invalid(ParsedArgs) -> ParsedArgs\|Exit(1)`. bash `while/case` over argv (long-flag form mirroring `decision-log/record.sh`); enumerates known flags (`--slug --id --title --chosen --actor --rationale --alternatives --status --iteration --blocking --timestamp --root`) and rejects `--delete/--remove/--purge/--supersede` plus any unknown flag with exit 2. Applies FR-10 defaults: `iteration=1`, `status=proposed`, `blocking=false`, `timestamp=date -u +%Y-%m-%dT%H:%M:%SZ` (UTC regardless of TZ), `root=$(pwd)/planning`. Validates: `actor in {human,ai}`, `status in {proposed,accepted,superseded}`, `iteration` matches `^[0-9]+$` (EC-5: 0 and 2147483647 both accepted), `blocking` literal `true\|false`, `timestamp` matches `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$` (EC-19). refuse_on_missing: first-missing-field rule over fixed order `[SLUG, DEC_ID, TITLE, CHOSEN, ACTOR]`; stderr names ONE field, NEVER echoes a supplied value (FR-3/FR-9). Pattern reused verbatim from `.claude/skills/cfn-megaplan/bars/bless-verify.sh:80-94`. | `.claude/skills/cfn-decisions/lib/arg-parse.sh:parse_and_validate_args`, `.claude/skills/cfn-decisions/lib/arg-parse.sh:refuse_on_missing_or_invalid` | - | TDD step 13 (AC-20 + AC-58 exit 2 / EC-19; unit) | `bash .claude/skills/cfn-decisions/tests/31-exit-2-cli-parse.sh 2>&1 \| tee "$OUT"` | test green; refuse_on_missing iterates fixed field order; no supplied value ever reaches stderr |
| A.4 | `.claude/skills/cfn-decisions/lib/jq-build.sh` | OP-W2 `build_decision_object(ParsedArgs) -> ENTRY\|Exit(3)`. Builds ENTRY via `jq -n --arg id/actor/title/chosen/rationale/alternatives/status/timestamp --argjson iteration/blocking '{...}'`. NEVER string-concatenate JSON (FR-6). On jq exit non-zero OR ENTRY fails `jq empty`: exit 3 (`internal: jq failed to build decision object`). The exit-3 codepath is defensive/near-unreachable because `--arg` escapes every untrusted string (documented in test AC-21). Pattern reused from `bless-verify.sh:137-141`. Hostile-input guarantee: EC-13 (JSON breakout `","evil":true,"gap":"`) persists as flat string, no `evil` key; EC-14 (`<script>` + `; DROP TABLE`) persists verbatim; EC-21 (emoji 🦀 + RLO ‮ + CJK + surrogate) round-trips byte-equal; EC-22 (em dash U+2014) persists verbatim (NFR-5 carve-out: ban is on writer's OWN code, not caller data); EC-4 (10000 chars) persisted in full. | `.claude/skills/cfn-decisions/lib/jq-build.sh:build_decision_object` | - | TDD steps 7, 19, 20, 21, 22 (AC-8 jq / AC-45 SQL / AC-46 unicode / AC-47 10k / AC-48 em-dash; unit) | `bash .claude/skills/cfn-decisions/tests/60-jq-construction.sh && bash .claude/skills/cfn-decisions/tests/61-sql-injection.sh && bash .claude/skills/cfn-decisions/tests/62-unicode-roundtrip.sh && bash .claude/skills/cfn-decisions/tests/63-oversized-10k.sh && bash .claude/skills/cfn-decisions/tests/64-em-dash-caller.sh 2>&1 \| tee "$OUT"` | all 5 tests green; static grep `grep -nP '\x{2014}' lib/jq-build.sh` returns zero em dashes in writer's own code; AC-21 source-grep `grep -nE 'exit 3\|E_JQ_BUILD' lib/jq-build.sh` matches the codepath |
| A.5 | `.claude/skills/cfn-decisions/lib/upsert.sh` | OP-W3 `upsert_by_key_atomic(ParsedArgs, ENTRY) -> TARGET\|Exit(4\|5)`. Derive: `DIR="$ROOT"`, `BASE="VERIFY_${SLUG}"`, `TARGET="$DIR/.$BASE.decisions.json"` (i.e. `planning/.VERIFY_<slug>.decisions.json`). If DIR missing/RO: exit 4 (`planning dir missing or read-only: <dir>`). `TMP=$(mktemp "$DIR/.dec.XXXXXX")`; on mktemp fail: exit 4 (`mktemp failed in <dir>`). INSTALL cleanup trap: `trap '[ -n "${TMP:-}" ] && rm -f "$TMP"' EXIT INT TERM` (FR-4; `${TMP:-}` default guards the unset-before-set case under `set -u` in the 5b path). Branch on TARGET state: (5a) absent -> `jq -n --arg slug "$SLUG" --argjson new "$ENTRY" '{slug:$slug, decisions:[$new]}' > "$TMP"` (bootstrap wrapper, never bare array); (5b) exists AND `jq empty "$TARGET"` fails -> `rm -f "$TMP"`; exit 5 (`existing <target> is not valid JSON; refusing overwrite`); PRESERVE the bad file for inspection. (5c) exists and valid -> upsert by id: `jq --argjson new "$ENTRY" 'if (.decisions // [] \| map(.id) \| index($new.id)) != null then .decisions = (.decisions \| map(if .id == $new.id then $new else . end)) else .decisions += [$new] end' "$TARGET" > "$TMP"` (FR-2 replace-by-key; preserves other elements and their relative order; EC-16 any-status-to-any-status legal). If jq step failed OR `jq empty "$TMP"` fails: `rm -f "$TMP"`; exit 5 (`internal: jq upsert produced invalid JSON`). `mv "$TMP" "$TARGET"`; on mv fail: `rm -f "$TMP"` best-effort; exit 4 (`mv failed to commit <target>`). Pattern extended from `bless-verify.sh:62-66,137-149` (DIR/BASE/mktemp+mv) but UPSERT-BY-KEY instead of append-only (SPEC FR-2: decisions are entities, not events). Concurrency (EC-6): last-writer-wins via POSIX `rename(2)` atomic mv; per-writer mktemp; per-writer trap. | `.claude/skills/cfn-decisions/lib/upsert.sh:upsert_by_key_atomic` | - | TDD steps 1, 2, 3, 14, 15, 23, 24, 25 (AC-2 CARDINALITY RED FIRST / AC-4 atomic RED FIRST / AC-1 insert / AC-22 FS / AC-23 corrupt / AC-49 race / AC-50 reader / AC-59 DST; integration) | `bash .claude/skills/cfn-decisions/tests/20-upsert-by-key.sh && bash .claude/skills/cfn-decisions/tests/40-atomic-write.sh && bash .claude/skills/cfn-decisions/tests/10-insert-new.sh && bash .claude/skills/cfn-decisions/tests/33-exit-4-filesystem.sh && bash .claude/skills/cfn-decisions/tests/34-exit-5-target-corrupt.sh && bash .claude/skills/cfn-decisions/tests/41-concurrency-race.sh && bash .claude/skills/cfn-decisions/tests/42-reader-during-write.sh && bash .claude/skills/cfn-decisions/tests/43-dst-boundary.sh 2>&1 \| tee "$OUT"` | all 8 tests green; AC-2 specifically asserts array length stays UNCHANGED at 3 after re-resolve (NOT append); AC-4 asserts no partial observable after `kill -9` mid-write |
| A.6 | `.claude/skills/cfn-decisions/lib/sink-delegate.sh` | OP-W4 `delegate_to_record_sh(ParsedArgs, TARGET) -> SQLITE_SYNCED\|Exit(7\|8)`. PRECONDITION: OP-W3 already committed TARGET. If `command -v record.sh >/dev/null 2>&1` fails (EC-10): D-7 winner 2a-PERSIST -> `printf 'record.sh missing; JSON persisted at %s; SQLite sync skipped\n' "$TARGET" >&2`; exit 7 (NEVER 6; never roll back JSON). Build ARGV_SQL with SHARED fields only: `[--slug, SLUG, --id, DEC_ID, --title, TITLE, --chosen, CHOSEN, --rationale, RATIONALE, --alternatives, ALTS, --status, STATUS, --timestamp, TS]`. ALWAYS forward `--status` explicitly (sink defaults `accepted` at `record.sh:19`; writer defaults `proposed`; forgetting would silently upgrade). If `BLOCKING_BOOL=true`: append bare `--blocking` flag (sink uses bare-flag form, `record.sh:30`). NEVER pass `--project` (Q-6: sink derives from git toplevel basename). NEVER pass `--actor` or `--iteration` (FR-5: JSON-only, run-scoped). NEVER pass `--supersede` (status encoded via `--status`). Invoke `record.sh "${ARGV_SQL[@]}" 2>/dev/null` (suppress sink stderr to enforce FR-9 even if sink misbehaves). If RC=0: return SQLITE_SYNCED. Else (EC-8): `printf 'record.sh failed exit=%d; JSON persisted at %s; SQLite out of sync\n' "$RC" "$TARGET" >&2`; exit 8 (rationale NOT echoed; id+TARGET only). Idempotent retry safe. | `.claude/skills/cfn-decisions/lib/sink-delegate.sh:delegate_to_record_sh` | `.claude/skills/decision-log/record.sh` | TDD steps 8, 9, 10 (AC-5 happy / AC-6 sink-nonzero / AC-7 sink-missing; integration) | `bash .claude/skills/cfn-decisions/tests/50-dual-write-happy.sh && bash .claude/skills/cfn-decisions/tests/51-dual-write-sink-nonzero.sh && bash .claude/skills/cfn-decisions/tests/52-dual-write-sink-missing.sh 2>&1 \| tee "$OUT"` | all 3 tests green; stderr shape matches ARCH §10.2 regex; AC-6 asserts JSON entry still queryable after sink failure (KEPT, not rolled back); Consumes `.claude/skills/decision-log/record.sh` is a LOCKED subprocess (called, never modified) |
| A.7 | `.claude/skills/cfn-decisions/lib/help.sh` + exit-code documentation + FR-8 no-DELETE surface audit + FR-9 no-leak floor | `print_help() -> stdout` (no side effects; no file mod). Output documents all 12 flags from ARCH §2.1 required-vs-optional table; lists reject flags (`--delete`, `--remove`, `--purge`, `--supersede`) with reason `unknown arg` shape; documents exit codes 0..5, 7, 8 with code 6 RESERVED (D-7 comment explaining why never emitted). NO `DELETE` SQL, NO `rm` of target file, NO `truncate`, NO `--delete` flag in `--help` output (FR-8 floor `no_unscoped_delete`). NO rationale echoed on stdout/stderr (FR-9 floor `secrets_handling`, `pii_if_present`). The lib also hosts the exit-code constants comment block (`E_OK=0, E_VALIDATION=1, E_CLI_PARSE=2, E_JQ_BUILD=3, E_FILESYSTEM=4, E_TARGET_CORRUPT=5, E_RESERVED_6=6, E_SINK_MISSING=7, E_SINK_NONZERO=8`) as the single source of truth for ARCH §10.1. | `.claude/skills/cfn-decisions/lib/help.sh:print_help` | - | TDD steps 11, 12, 16, 17 (AC-14 no-delete / AC-15 no-leak floor / AC-21 exit 3 / AC-24 exit 6 reserved; static + integration) | `bash .claude/skills/cfn-decisions/tests/80-no-delete-surface.sh && bash .claude/skills/cfn-decisions/tests/90-no-leak-floor.sh && bash .claude/skills/cfn-decisions/tests/32-exit-3-jq-build.sh && bash .claude/skills/cfn-decisions/tests/35-exit-6-reserved.sh 2>&1 \| tee "$OUT"` | all 4 tests green; static grep `grep -nE '\bDELETE\b\|rm[[:space:]]+-[rf]*f?\|truncate' .claude/skills/cfn-decisions/record.sh .claude/skills/cfn-decisions/lib/*.sh` returns zero matches; `record.sh --help` output contains no `--delete`; AC-15 leak marker `secret-marker-ZZY-12345` appears ONLY in JSON + SQLite (OBS-5 pattern from ARCH §10.4) |
| A.8 | `.claude/skills/cfn-decisions/lib/state-machine-doc.md` + `.claude/skills/cfn-decisions/tests/run-all.sh` aggregator + volume/perf/renderer tests + WIRE-1/3/4 (writer, sink, renderer) + SM-1..SM-9 + OBS-6/7 derived + assembled-prep fixtures (simulated OP-H1 wrapper per ARCH §3) | state-machine-doc.md mirrors ARCH §9 verbatim: SM-1..SM-9 valid transitions table, illegal-transition table EMPTY by design (EC-16: all status pairs legal both directions), mermaid diagram, mechanics notes (upsert replaces; relative order preserved; iteration caller-supplied; NO DELETE; sink `--supersede` not used by writer). `tests/run-all.sh`: invokes each `tests/*.sh` in TDD-ordering sequence (TEST Phase 6 order: AC-2 first, AC-4 second, then the rest); propagates failure; emits `PASS <name>` / `FAIL <name>` lines; `set -uo pipefail`. WIRE-1: assert writer file exists + executable + real logic (>=50 lines). WIRE-3: assert writer invokes sink by relative path or PATH lookup with documented flags. WIRE-4: assert renderer unchanged (LOCKED; sha256 of `section-decisions.sh` == `97ec8ef0f0bccebc8ff0d0024cf85a59b9cc9fe21d23a8aa8f725264417f2fb6` pinned at plan time per Q3; drift turns red by design). Volume/perf ACs (AC-55 100-row, AC-60 1000-row) verify NFR-3 p95 < 500ms. OBS-6 derived test: JSON count == OBS-1 line count. OBS-7 derived: divergence count == `record.sh failed exit=` line count. Assembled-prep fixtures: bash simulation of the OP-H1 wrapper (record_decision function from PSEUDO OP-H1) inline in tests, matching ARCH §1 note "no new file for the wrapper". | `.claude/skills/cfn-decisions/lib/state-machine-doc.md`, `.claude/skills/cfn-decisions/tests/run-all.sh` | `.claude/skills/cfn-decisions/record.sh`, `.claude/skills/decision-log/record.sh`, `.claude/skills/cfn-workbench/lib/section-decisions.sh` | TDD steps 18, 26, 27, 28, 29, 30, 32, 33, 40, 41 (AC-25..34 SM / AC-52 renderer / AC-54 manual / AC-55 100-row / AC-60 1000-row / WIRE-1 / WIRE-3 / WIRE-4 / OBS-6 / OBS-7; integration + perf + wiring) | `bash .claude/skills/cfn-decisions/tests/run-all.sh 2>&1 \| tee "$OUT"` AND `bash .claude/skills/cfn-decisions/tests/40-sm-transitions.sh && bash .claude/skills/cfn-decisions/tests/70-renderer-contract.sh && bash .claude/skills/cfn-decisions/tests/71-manual-invocation.sh && bash .claude/skills/cfn-decisions/tests/81-volume-100.sh && bash .claude/skills/cfn-decisions/tests/82-volume-1000.sh && bash .claude/skills/cfn-decisions/tests/w-1-writer-wired.sh && bash .claude/skills/cfn-decisions/tests/w-3-sink-wired.sh && bash .claude/skills/cfn-decisions/tests/w-4-renderer-wired.sh && bash .claude/skills/cfn-decisions/tests/obs-6-parity.sh && bash .claude/skills/cfn-decisions/tests/obs-7-divergence.sh 2>&1 \| tee "$OUT"` | all tests green; run-all.sh exits 0; SM-1..SM-9 each have a green sub-assertion; WIRE-1/3/4 static greps pass; p95 < 500ms at 1000 entries; OBS-6 parity holds; OBS-7 divergence == count of OBS-3 lines; all three Consumes are read-only asserts (writer does not modify record.sh, decision-log/record.sh, or section-decisions.sh) |
| A.9 | `.claude/skills/cfn-decisions/record.sh` + `.claude/skills/cfn-decisions/lib/sink-delegate.sh` | Q1 PROMOTED (Step 7 deferred-decision, user-approved 2026-07-28): add hang-resistance to the sink delegation. Add canonical constant `SINK_TIMEOUT_SECONDS=30` to the writer constants block in `record.sh` (named constant per code-quality rule, not a magic number). Wrap the sink subprocess call in `delegate_to_record_sh` (step A.6) as `timeout "${SINK_TIMEOUT_SECONDS:-30}" .claude/skills/decision-log/record.sh "${ARGV_SQL[@]}" 2>/dev/null`. The `timeout` invocation AND `SINK_TIMEOUT_SECONDS` constant MUST appear in `record.sh` to satisfy AC-65 static grep (the implementer picks the cleanest form: inline the wrapped call from `record.sh`, or surface the pattern via a sourced wrapper; either way the grep on `record.sh` must find it). DRY: one change covers all 4 coordinator hook sites (they invoke this writer, so the timeout is centralized here, NOT at the 4 coordinator sites). Chain on hang: timeout kills sink (exit 124), writer treats 124 as non-zero RC per A.6 EC-8 branch, keeps JSON per D-7, exits sync-failed (exit 8), hook site D-8 isolation logs + continues. No change to exit-code taxonomy (124 from timeout maps to existing exit 8 E_SINK_NONZERO). | `.claude/skills/cfn-decisions/record.sh:SINK_TIMEOUT_SECONDS` | `.claude/skills/decision-log/record.sh` | TDD step 42 (AC-65 timeout wrapper present; static) + step 43 (AC-66 sink-hang behavior; integration) | `grep -cE 'timeout .*SINK_TIMEOUT_SECONDS' .claude/skills/cfn-decisions/record.sh` >= 1 AND `bash .claude/skills/cfn-decisions/tests/53-sink-hang-timeout.sh 2>&1 \| tee "$OUT"` | AC-65 grep >= 1; AC-66 stub `record.sh` to `sleep 60`, invoke writer, assert wall time < 32s AND JSON entry preserved AND writer exit non-zero (sync-failed) |

### Lane B: Coordinator Hooks (consumer, SERIAL AFTER Lane A)

Lane B starts ONLY after Lane A's `bash .claude/skills/cfn-decisions/tests/run-all.sh` exits 0 (OPS §3 Stage 1 promote gate). Lane B edits prose+snippet at 4 sites; the OP-H1 wrapper code lives inline in markdown (ARCH §1: "no new file"). Each site consumes `.claude/skills/cfn-decisions/record.sh` (the identifier Lane A step A.2 Produces) and emits OBS-1 (success) or OBS-4 (isolation warning per D-8).

| # | File (full path) | Change (exact: function name, typed signature, or config key) | Produces | Consumes | Failing test (TEST Phase 6) | Verify command (exits 0/1) | Done predicate |
|---|---|---|---|---|---|---|---|
| B.1 | `.claude/commands/cfn-loop-task.md` | SITE 1 Phase 4.2 product-owner 2/3 (anchor line 414-416): insert OP-H1 wrapper invocation BETWEEN the product-owner verdict receipt AND "Mark todo #4 as completed" dispatch. One hook call per resolved 2/3 item, inside the "consult product-owner one at a time" loop. Wrapper: `record_decision(slug, dec_id, title, chosen, rationale, alts, status, actor, iteration, blocking, ts, root, site_id)` (per PSEUDO OP-H1). SITE 1 mapping: actor=ai, status: IMPLEMENT->accepted / DEFER->proposed / REJECT->superseded, blocking=true if vote was block-severity else false. Wrapper body: invoke `.claude/skills/cfn-decisions/record.sh` with mapped argv; on RC=0 emit `decisions.ledger id=<id> status=<status>\n` to stdout; on RC!=0 emit `decisions.ledger id=<id> record FAILED rc=<n> site=phase-4.2-po\n` to stderr AND `return 0` (D-8 isolation: the decision was made by the product-owner agent; losing the ledger row is a coverage gap not a wrong decision; loop continues). The hook is ADDITIVE: no existing record call at this site; it cannot change the verdict or the IMPLEMENT/DEFER/REJECT dispatch. | `.claude/commands/cfn-loop-task.md:SITE-1-phase-4.2-po-hook` | `.claude/skills/cfn-decisions/record.sh` | TDD step 34 (AC-9 FR-7 SITE 1 + OBS-1; assembled) | `bash .claude/skills/cfn-decisions/tests/70-hook-site-1-po.sh 2>&1 \| tee "$OUT"` | test green; static grep `grep -c 'cfn-decisions/record.sh' .claude/commands/cfn-loop-task.md` >= 3 (this step adds 1,后续 steps B.2/B.3 add the rest); simulated wrapper emits one OBS-1 line with `status=accepted` for an IMPLEMENT verdict; JSON entry exists with `actor=ai blocking=true` for a block-severity vote |
| B.2 | `.claude/commands/cfn-loop-task.md` | SITE 2 Phase 5 user-batch (anchor line 429-439): insert OP-H1 wrapper invocation BETWEEN each `AskUserQuestion` batch return AND "implement the Apply items" dispatch. One hook call per item in the returned batch (4 items max per batch per the batched-4 rule). SITE 2 mapping: actor=human, status: Apply->accepted / Skip->superseded / Defer->proposed, blocking=false (1/3 items are by definition non-blocking per routing table at line 411). Wrapper same shape as B.1 with `site_id=phase-5-batch`. ON-RECORD-FAILURE: D-8 isolate (the user's choice is honored regardless of ledger state). | `.claude/commands/cfn-loop-task.md:SITE-2-phase-5-batch-hook` | `.claude/skills/cfn-decisions/record.sh` | TDD step 35 (AC-10 FR-7 SITE 2; assembled) | `bash .claude/skills/cfn-decisions/tests/71-hook-site-2-batch.sh 2>&1 \| tee "$OUT"` | test green; for a 3-item batch the simulated wrapper invokes the writer 3 times; 3 OBS-1 lines with distinct ids; JSON has 3 entries with mapped statuses |
| B.3 | `.claude/commands/cfn-loop-task.md` | SITE 3 Phase 5E.4 quarantine (anchor line 535-538): insert OP-H1 wrapper invocation BETWEEN the user's Quarantine/Keep-iterating/Abort choice AND the bullet-list dispatch (test.skip wrap / Phase 2 return / report+exit). One hook call total (one decision by design). SITE 3 mapping: actor=human, status: Quarantine->accepted / Keep iterating->proposed / Abort->superseded, blocking=true (quarantine is load-bearing for Step 3.05 hygiene). ON-RECORD-FAILURE: D-8 isolate at this site too (D-8 OVERRIDES the prior "load-bearing, must propagate" reasoning: the audit row is an audit side-effect, not a gate on the quarantine itself; test.skip still happens, backlog entry still lands). site_id=phase-5E.4-quarantine. | `.claude/commands/cfn-loop-task.md:SITE-3-phase-5E.4-quarantine-hook` | `.claude/skills/cfn-decisions/record.sh` | TDD step 36 (AC-11 FR-7 SITE 3; assembled) | `bash .claude/skills/cfn-decisions/tests/72-hook-site-3-quarantine.sh 2>&1 \| tee "$OUT"` | test green; static grep now `grep -c 'cfn-decisions/record.sh' .claude/commands/cfn-loop-task.md` >= 3 (all 3 loop sites wired); simulated wrapper emits OBS-1 with `status=accepted blocking=true` for a Quarantine choice |
| B.4 | `.claude/skills/cfn-megaplan/SKILL.md` | SITE 4 megaplan L3 decide (anchor line 215): REWRITE the existing sentence at line 215 that routes BLOCKING `[OPEN]` resolutions to `cfn-decide` (the L3 phase skill; current text reads `cfn-decide owns the register`). New sentence (D-9): "If any phase returns **BLOCKING** `[OPEN]` items, batch them and surface via `AskUserQuestion` before advancing past the level. Record every resolved decision via `.claude/skills/cfn-decisions/record.sh` (closes gap G35/decision-log loop AND populates the per-run JSON ledger). The writer delegates the SQLite register sync to `decision-log/record.sh`." This is the ONE substitution site (D-9): behavior-preserving for SQLite (writer delegates to record.sh per D-1; same row lands with same fields) and additive for JSON (a new entry appears in `.VERIFY_<slug>.decisions.json`). SITE 4 mapping: actor=human (BLOCKING [OPEN] resolves via user AskUserQuestion), status=accepted (canonical accepted-decision shape), blocking=true (BLOCKING items are blocking by definition). site_id=megaplan-L3-decide. Wrapper same shape as B.1..B.3. ON-RECORD-FAILURE: D-8 isolate (the user's decision must advance the level even if the ledger write fails). | `.claude/skills/cfn-megaplan/SKILL.md:SITE-4-megaplan-L3-decide-hook` | `.claude/skills/cfn-decisions/record.sh` | TDD step 37 (AC-12 FR-7 SITE 4 / D-9 substitution / WIRE-2; assembled) | `bash .claude/skills/cfn-decisions/tests/73-hook-site-4-megaplan.sh 2>&1 \| tee "$OUT"` | test green; static grep `grep -c 'cfn-decisions/record.sh' .claude/skills/cfn-megaplan/SKILL.md` >= 1; the simulated wrapper asserts BOTH a JSON entry AND a SQLite row land for the same id (D-9 behavior-preserving proof); POST-EDIT line 215: `grep -c 'cfn-decisions' <(sed -n '215p' .claude/skills/cfn-megaplan/SKILL.md)` >= 1 AND `grep -wc 'cfn-decide' <(sed -n '215p' .claude/skills/cfn-megaplan/SKILL.md)` == 0 (new writer ref present; old skill-name token absent) |
| B.5 | `.claude/commands/cfn-loop-task.md` + `.claude/skills/cfn-megaplan/SKILL.md` | D-8 isolation harness across ALL 4 sites (cross-cutting). The wrapper template body (record_decision function) lives inline at each site; the wrapper's failure branch returns 0 unconditionally on any writer non-zero exit code (1, 2, 3, 4, 5, 7, 8). The wrapper emits OBS-4 `decisions.ledger id=<id> record FAILED rc=<n> site=<site_id>` to stderr. After the wrapper's `return 0`, the coordinator proceeds (post-site marker appears in run-log: a `phase continued` echo in the assembled test). | - (verification-only step; no new file, edits inline at all 4 sites' wrapper bodies) | `.claude/skills/cfn-decisions/record.sh` | TDD step 38 (AC-13 + AC-38 D-8 isolation / OBS-4; assembled) + step 31 (WIRE-2 all 4 sites wired) + step 39 (OBS-1 success line) | `bash .claude/skills/cfn-decisions/tests/74-hook-isolation-d8.sh && bash .claude/skills/cfn-decisions/tests/w-2-hook-wired.sh && bash .claude/skills/cfn-decisions/tests/obs-1-success-line.sh 2>&1 \| tee "$OUT"` | tests green; for each of the 4 sites, forcing a writer failure (missing `--title`) emits an OBS-4 line AND a post-site `phase continued` marker; static grep totals: `grep -c 'cfn-decisions/record.sh' .claude/commands/cfn-loop-task.md` >= 3 AND `grep -c 'cfn-decisions/record.sh' .claude/skills/cfn-megaplan/SKILL.md` >= 1 (WIRE-2 across all 4 sites); OBS-1 fires one success line per resolved decision in a 3-decision simulated run |

## Ops Integration Tasks

Per the write-plan skill: one implementation-step row per OPS section 3 (flag), EVERY OBS signal emit step (OPS §2), and the down-migration file (OPS §6). The OBS-* rows bind to existing Lane A / Lane B steps where the emit lives; this section makes the binding explicit.

| OPS item | Implementation step that emits/verifies it | Emit mechanism | Bound test |
|---|---|---|---|
| Feature flag (OPS §3) | (none; n/a) | n/a: D-8 isolation discharges the safety requirement a flag would otherwise cover (OPS §3 line 202). No feature flag added. The kill-switch is `git revert` of Lane B commits (OPS §6 step 1). | n/a (no flag to test) |
| OBS-1 success log line | Lane B step B.5 (wrapper body) | `printf 'decisions.ledger id=%s status=%s\n' "$dec_id" "$status"` to stdout on writer RC=0 | AC-35 (`tests/obs-1-success-line.sh`) |
| OBS-2 stderr names field | Lane A step A.3 (`lib/arg-parse.sh:refuse_on_missing_or_invalid`) | `printf 'missing required field: %s\n' "$fieldname" >&2` (exit 1); constraint-violation variants exit 2 (e.g. `actor must be human\|ai`) | AC-36 (covered by AC-3 `tests/30-refuse-missing.sh`) |
| OBS-3 stderr on SQLite-sync failure | Lane A step A.6 (`lib/sink-delegate.sh`) | Two shapes: `record.sh missing; JSON persisted at <target>; SQLite sync skipped` (exit 7); `record.sh failed exit=<n>; JSON persisted at <target>; SQLite out of sync` (exit 8). Both to stderr. Sink's own stderr suppressed via `2>/dev/null` on the subprocess to enforce FR-9 even if the sink misbehaves. | AC-37 (covered by AC-6 + AC-7 `tests/51-dual-write-sink-nonzero.sh` + `tests/52-dual-write-sink-missing.sh`) |
| OBS-4 hook isolation warning | Lane B step B.5 (wrapper failure branch, ALL 4 sites) | `printf 'decisions.ledger id=%s record FAILED rc=%d site=%s\n' "$dec_id" "$RC" "$site_id" >&2` then `return 0` (D-8) | AC-38 (covered by AC-13 `tests/74-hook-isolation-d8.sh`) |
| OBS-5 rationale-absent invariant | Lane A step A.7 (FR-9 no-leak floor) | Negation signal: NO emit. Enforced by `grep -rF secret-marker-ZZY-12345 /tmp` returning zero matches; stdout/stderr/run-log clean; marker present ONLY in JSON + SQLite. | AC-39 (covered by AC-15 `tests/90-no-leak-floor.sh`) |
| OBS-6 captured-decisions parity (derived) | Lane A step A.8 (test-side derivation) | Derived: `jq '.decisions \| length' planning/.VERIFY_<slug>.decisions.json` == count of OBS-1 success lines in run-log. No emit; computed in test. | AC-40 (`tests/obs-6-parity.sh`) |
| OBS-7 sync divergence counter (derived) | Lane A step A.8 (test-side derivation) | Derived: `abs(JSON_count - SQLite_count)`; non-zero value is the trigger to investigate OBS-3/OBS-4. No emit; computed in test. | AC-41 (`tests/obs-7-divergence.sh`) |
| Down-migration (OPS §6) | (no migration file; db=no) | n/a: `db=no`; the writer owns no schema (OPS §6 line 275 confirms no `cfn-migration-rehearsal` invocation). Rollback rehearsal is 4 git/filesystem steps (OPS §6): (1) `git revert` Lane B commits; (2) `rm -rf .claude/skills/cfn-decisions/`; (3) stale JSON files INERT (renderer tolerates them); (4) SQLite rows persist (LOCKED sink, separate lifecycle). Zero data deletion. | n/a (no migration to rehearse; rollback rehearsal is reasoning-trace per OPS §6 line 273) |

## TDD Sequence (per step, mechanical)

Inherited VERBATIM from `planning/TEST_decisions_ledger.md` Phase 6. Each row binds to an implementation step # from Implementation Steps above. Do NOT invent new test cases. Execution rule per step: (1) write the test, (2) coordinator runs Red command, confirm non-zero, (3) implement the change in the bound step row, (4) coordinator runs Green command, confirm 0. The coordinator executes test commands; agents write tests and read results.

| Step # | Failing test written FIRST (file::case) | Red command (must exit non-zero) | Green command (must exit 0 after step) | Runnable-at | Lane.step |
|---|---|---|---|---|---|
| 1 | `tests/20-upsert-by-key.sh` (AC-2, FR-2 CARDINALITY) | `bash tests/20-upsert-by-key.sh` | `bash tests/20-upsert-by-key.sh` | integration | A.5 |
| 2 | `tests/40-atomic-write.sh` (AC-4, FR-4 atomic) | `bash tests/40-atomic-write.sh` | `bash tests/40-atomic-write.sh` | integration | A.5 |
| 3 | `tests/10-insert-new.sh` (AC-1, FR-1 insert) | `bash tests/10-insert-new.sh` | `bash tests/10-insert-new.sh` | integration | A.5 |
| 4 | `tests/95-defaults.sh` (AC-16 + AC-53 + AC-57, FR-10 defaults) | `bash tests/95-defaults.sh` | `bash tests/95-defaults.sh` | unit | A.2 |
| 5 | `tests/96-actor-enum.sh` (AC-17, FR-10 actor enum) | `bash tests/96-actor-enum.sh` | `bash tests/96-actor-enum.sh` | unit | A.2 |
| 6 | `tests/30-refuse-missing.sh` (AC-3 + AC-42, FR-3 per-field) | `bash tests/30-refuse-missing.sh` | `bash tests/30-refuse-missing.sh` | unit | A.2 |
| 7 | `tests/60-jq-construction.sh` (AC-8 + AC-44, FR-6 + XSS half of EC-14) | `bash tests/60-jq-construction.sh` | `bash tests/60-jq-construction.sh` | unit | A.4 |
| 8 | `tests/50-dual-write-happy.sh` (AC-5, FR-5 happy) | `bash tests/50-dual-write-happy.sh` | `bash tests/50-dual-write-happy.sh` | integration | A.6 |
| 9 | `tests/51-dual-write-sink-nonzero.sh` (AC-6 + AC-37, FR-5 D-7 / OBS-3) | `bash tests/51-dual-write-sink-nonzero.sh` | `bash tests/51-dual-write-sink-nonzero.sh` | integration | A.6 |
| 10 | `tests/52-dual-write-sink-missing.sh` (AC-7 + AC-37, FR-5 D-7 missing / OBS-3) | `bash tests/52-dual-write-sink-missing.sh` | `bash tests/52-dual-write-sink-missing.sh` | integration | A.6 |
| 11 | `tests/80-no-delete-surface.sh` (AC-14 + AC-56, FR-8 + EC-17) | `bash tests/80-no-delete-surface.sh` | `bash tests/80-no-delete-surface.sh` | static (wiring-stage) | A.7 |
| 12 | `tests/90-no-leak-floor.sh` (AC-15 + AC-39, FR-9 floor / OBS-5) | `bash tests/90-no-leak-floor.sh` | `bash tests/90-no-leak-floor.sh` | integration | A.7 |
| 13 | `tests/31-exit-2-cli-parse.sh` (AC-20 + AC-58, exit 2 / EC-19) | `bash tests/31-exit-2-cli-parse.sh` | `bash tests/31-exit-2-cli-parse.sh` | unit | A.3 |
| 14 | `tests/33-exit-4-filesystem.sh` (AC-22 + AC-51, exit 4 / EC-9 / EC-11 / EC-24) | `bash tests/33-exit-4-filesystem.sh` | `bash tests/33-exit-4-filesystem.sh` | integration | A.5 |
| 15 | `tests/34-exit-5-target-corrupt.sh` (AC-23, exit 5) | `bash tests/34-exit-5-target-corrupt.sh` | `bash tests/34-exit-5-target-corrupt.sh` | integration | A.5 |
| 16 | `tests/32-exit-3-jq-build.sh` (AC-21, exit 3 defensive) | `bash tests/32-exit-3-jq-build.sh` | `bash tests/32-exit-3-jq-build.sh` | static (source grep; runtime near-unreachable) | A.7 |
| 17 | `tests/35-exit-6-reserved.sh` (AC-24, exit 6 never emitted) | `bash tests/35-exit-6-reserved.sh` | `bash tests/35-exit-6-reserved.sh` | static | A.7 |
| 18 | `tests/40-sm-transitions.sh` (AC-25..AC-34, SM-1..SM-9 + illegal-table-empty) | `bash tests/40-sm-transitions.sh` | `bash tests/40-sm-transitions.sh` | integration | A.8 |
| 19 | `tests/61-sql-injection.sh` (AC-45, EC-14 SQL half) | `bash tests/61-sql-injection.sh` | `bash tests/61-sql-injection.sh` | integration (stub record.sh records argv) | A.4 |
| 20 | `tests/62-unicode-roundtrip.sh` (AC-46 + ADV-1, EC-21) | `bash tests/62-unicode-roundtrip.sh` | `bash tests/62-unicode-roundtrip.sh` | unit | A.4 |
| 21 | `tests/63-oversized-10k.sh` (AC-47 + ADV-2, EC-4) | `bash tests/63-oversized-10k.sh` | `bash tests/63-oversized-10k.sh` | unit + perf | A.4 |
| 22 | `tests/64-em-dash-caller.sh` (AC-48, EC-22 / NFR-5) | `bash tests/64-em-dash-caller.sh` | `bash tests/64-em-dash-caller.sh` | unit + static | A.4 |
| 23 | `tests/41-concurrency-race.sh` (AC-49, EC-6) | `bash tests/41-concurrency-race.sh` | `bash tests/41-concurrency-race.sh` | integration | A.5 |
| 24 | `tests/42-reader-during-write.sh` (AC-50, EC-7) | `bash tests/42-reader-during-write.sh` | `bash tests/42-reader-during-write.sh` | integration | A.5 |
| 25 | `tests/43-dst-boundary.sh` (AC-59, EC-20) | `bash tests/43-dst-boundary.sh` | `bash tests/43-dst-boundary.sh` | integration | A.5 |
| 26 | `tests/70-renderer-contract.sh` (AC-52, renderer projection) | `bash tests/70-renderer-contract.sh` | `bash tests/70-renderer-contract.sh` | contract | A.8 |
| 27 | `tests/71-manual-invocation.sh` (AC-54, EC-12) | `bash tests/71-manual-invocation.sh` | `bash tests/71-manual-invocation.sh` | static | A.8 |
| 28 | `tests/81-volume-100.sh` (AC-55 + NFR-3, EC-15) | `bash tests/81-volume-100.sh` | `bash tests/81-volume-100.sh` | integration + perf | A.8 |
| 29 | `tests/82-volume-1000.sh` (AC-60 + NFR-3, EC-23) | `bash tests/82-volume-1000.sh` | `bash tests/82-volume-1000.sh` | integration + perf | A.8 |
| 30 | `tests/w-1-writer-wired.sh` (AC-61, WIRE-1) | `bash tests/w-1-writer-wired.sh` | `bash tests/w-1-writer-wired.sh` | wiring (static grep on composition root) | A.8 |
| 31 | `tests/w-2-hook-wired.sh` (AC-62, WIRE-2) | `bash tests/w-2-hook-wired.sh` | `bash tests/w-2-hook-wired.sh` | wiring (static grep on the 4 coordinator sites) | B.5 |
| 32 | `tests/w-3-sink-wired.sh` (AC-63, WIRE-3) | `bash tests/w-3-sink-wired.sh` | `bash tests/w-3-sink-wired.sh` | wiring (static grep on writer's record.sh delegation) | A.8 |
| 33 | `tests/w-4-renderer-wired.sh` (AC-64, WIRE-4) | `bash tests/w-4-renderer-wired.sh` | `bash tests/w-4-renderer-wired.sh` | wiring (static grep + git diff vs LOCKED baseline) | A.8 |
| 34 | `tests/70-hook-site-1-po.sh` (AC-9, FR-7 SITE 1) | `bash tests/70-hook-site-1-po.sh` | `bash tests/70-hook-site-1-po.sh` | assembled (simulated OP-H1 wrapper) | B.1 |
| 35 | `tests/71-hook-site-2-batch.sh` (AC-10, FR-7 SITE 2) | `bash tests/71-hook-site-2-batch.sh` | `bash tests/71-hook-site-2-batch.sh` | assembled | B.2 |
| 36 | `tests/72-hook-site-3-quarantine.sh` (AC-11, FR-7 SITE 3) | `bash tests/72-hook-site-3-quarantine.sh` | `bash tests/72-hook-site-3-quarantine.sh` | assembled | B.3 |
| 37 | `tests/73-hook-site-4-megaplan.sh` (AC-12, FR-7 SITE 4 / D-9) | `bash tests/73-hook-site-4-megaplan.sh` | `bash tests/73-hook-site-4-megaplan.sh` | assembled | B.4 |
| 38 | `tests/74-hook-isolation-d8.sh` (AC-13 + AC-38, FR-7 D-8 / OBS-4) | `bash tests/74-hook-isolation-d8.sh` | `bash tests/74-hook-isolation-d8.sh` | assembled | B.5 |
| 39 | `tests/obs-1-success-line.sh` (AC-35, OBS-1) | `bash tests/obs-1-success-line.sh` | `bash tests/obs-1-success-line.sh` | runtime-observed (run-log capture) | B.5 |
| 40 | `tests/obs-6-parity.sh` (AC-40, OBS-6 derived) | `bash tests/obs-6-parity.sh` | `bash tests/obs-6-parity.sh` | runtime-observed | A.8 |
| 41 | `tests/obs-7-divergence.sh` (AC-41, OBS-7 derived) | `bash tests/obs-7-divergence.sh` | `bash tests/obs-7-divergence.sh` | runtime-observed | A.8 |
| 42 | `tests/w-5-timeout-wrapper.sh` (AC-65, Q1 timeout wrapper present) | `bash tests/w-5-timeout-wrapper.sh` | `bash tests/w-5-timeout-wrapper.sh` | static (source grep for timeout at sink-call boundary) | A.9 |
| 43 | `tests/53-sink-hang-timeout.sh` (AC-66, Q1 sink-hang behavior) | `bash tests/53-sink-hang-timeout.sh` | `bash tests/53-sink-hang-timeout.sh` | integration (stub record.sh to sleep 60, assert wall time < 32s) | A.9 |

**Sequencing rules honored** (TEST §6):
- Unit reds first (steps 4..7, 13, 16..17, 19..22): pure argv / jq logic, no FS, no sink.
- Integration reds need the writer's filesystem + record.sh delegation (steps 1..3, 8..10, 14..15, 18, 23..29).
- Wiring guards (steps 30..33) land with the step that registers each component.
- Assembled-path rows for FR-7 (steps 34..38) sequence AFTER the writer is green AND AFTER WIRE-2 (Lane B).
- Runtime-observed rows (steps 39..41) sequence LAST: OBS-1 needs the wrapper in Lane B; OBS-6/7 need the OBS-1 lines plus the assembled wrapper running.
- Q1-promoted rows (steps 42..43) sequence after step A.9: step 42 is a static grep on `record.sh` for the timeout wrapper; step 43 needs the timeout wrapper live in the writer to assert the hang is killed at 30s (not 60s).

## Exit-Code Taxonomy (named implementing step + test per step)

ARCH §2.2 enum. Each code has a named step + test (per task brief requirement 4):

| Code | Constant | Meaning | Implementing step | Test |
|---|---|---|---|---|
| 0 | `E_OK` | success: JSON entry written AND SQLite row synced | A.2 (record.sh main success path) + A.5 (OP-W3 commits TARGET) + A.6 (OP-W4 returns SQLITE_SYNCED) | AC-18 (folded into AC-1 + AC-5) |
| 1 | `E_VALIDATION` | FR-3 missing/empty/whitespace required field | A.3 (lib/arg-parse.sh:refuse_on_missing_or_invalid) | AC-19 (covered by AC-3 `tests/30-refuse-missing.sh`) |
| 2 | `E_CLI_PARSE` | unknown flag, missing value, invalid enum, malformed timestamp/iteration/blocking | A.3 (lib/arg-parse.sh:parse_and_validate_args) + A.2 (jq startup check `command -v jq`) | AC-20 (`tests/31-exit-2-cli-parse.sh`) |
| 3 | `E_JQ_BUILD` | jq failed to build ENTRY (OP-W2; defensive, near-unreachable) | A.4 (lib/jq-build.sh:build_decision_object error branch) | AC-21 (`tests/32-exit-3-jq-build.sh`) |
| 4 | `E_FILESYSTEM` | dir missing/RO, mktemp fail, mv fail, disk full at FS layer | A.5 (lib/upsert.sh:upsert_by_key_atomic FS branches) | AC-22 (`tests/33-exit-4-filesystem.sh`) |
| 5 | `E_TARGET_CORRUPT` | existing `.VERIFY_<slug>.decisions.json` not valid JSON; refuse overwrite | A.5 (lib/upsert.sh branch 5b: PRESERVE bad file) | AC-23 (`tests/34-exit-5-target-corrupt.sh`) |
| 6 | `E_RESERVED_6` | RESERVED (was 2a-FATAL in PSEUDO OP-W4 step2a; REJECTED by D-7; never emitted) | A.7 (lib/help.sh comment block documents reservation; no `exit 6` call anywhere) | AC-24 (`tests/35-exit-6-reserved.sh` static source grep) |
| 7 | `E_SINK_MISSING` | `decision-log/record.sh` not on PATH (EC-10, D-7 PERSIST) | A.6 (lib/sink-delegate.sh branch 2: PERSIST) | AC-7 (`tests/52-dual-write-sink-missing.sh`) |
| 8 | `E_SINK_NONZERO` | `record.sh` exited non-zero (EC-8: SQLite busy, disk full at SQLite layer) | A.6 (lib/sink-delegate.sh branch 6: log + exit 8; JSON KEPT) | AC-6 (`tests/51-dual-write-sink-nonzero.sh`) |

## State Machine SM-1..SM-9 (named implementing step + test per step)

ARCH §9. All 9 valid transitions; 0 illegal (EC-16: all status pairs legal both directions; illegal-transition table EMPTY by design). Each SM-id has a named step + test (per task brief requirement 4):

| SM-id | From -> To | Trigger | Implementing step | Test |
|---|---|---|---|---|
| SM-1 | (absent) -> proposed | first invocation, `--status proposed` (FR-10 default) | A.5 (lib/upsert.sh bootstrap branch 5a) + A.3 (default status=proposed) | AC-25 (`tests/40-sm-transitions.sh -t sm1`) |
| SM-2 | (absent) -> accepted | first invocation, `--status accepted` | A.5 (bootstrap branch 5a) | AC-26 (sm2 case) |
| SM-3 | (absent) -> superseded | first invocation, `--status superseded` (rare) | A.5 (bootstrap branch 5a) | AC-27 (sm3 case) |
| SM-4 | proposed -> accepted | re-invoke same id, status accepted | A.5 (upsert branch 5c replace-in-place) | AC-28 (sm4 case) |
| SM-5 | proposed -> superseded | re-invoke, status superseded | A.5 (branch 5c) | AC-29 (sm5 case) |
| SM-6 | accepted -> proposed (EC-16 correction) | re-invoke, status proposed; NO forward-only gate | A.5 (branch 5c; no transition-rejection code) | AC-30 (sm6 case) |
| SM-7 | accepted -> superseded | re-invoke, status superseded | A.5 (branch 5c) | AC-31 (sm7 case) |
| SM-8 | superseded -> proposed (EC-16 correction) | re-invoke, status proposed | A.5 (branch 5c) | AC-32 (sm8 case) |
| SM-9 | superseded -> accepted (EC-16 correction) | re-invoke, status accepted | A.5 (branch 5c) | AC-33 (sm9 case) |
| illegal | (table EMPTY by design) | n/a: the writer enforces the `--status` enum (exit 2), NOT a directional gate; EC-16 forbids forward-only | A.3 (enum validation in parse_and_validate_args) | AC-34 (`tests/40-sm-transitions.sh` illegal-table case; static grep `grep -nE 'illegal transition\|cannot transition\|invalid state change' record.sh lib/*.sh` returns zero) |

## Hostile-Input EC Mapping (named step + test per EC)

Per task brief requirement 5: each hostile-input EC maps to a step + test.

| EC | Class | Implementing step (escapes/persists verbatim) | Test |
|---|---|---|---|
| EC-13 (JSON breakout `","evil":true,"gap":"`) | comma-injection | A.4 (lib/jq-build.sh: jq `-n --arg` escapes; no string concat) | AC-8 + AC-43 (`tests/60-jq-construction.sh`; assert `jq -e '.decisions[].evil'` returns null) |
| EC-14 (XSS `<script>` + SQL `; DROP TABLE decisions; --`) | html-script + sql-concat | A.4 (persists verbatim to JSON; writer does not strip/mangle/escape) + A.6 (passes to record.sh as ONE argv value, never as SQL; sink parameterizes) | AC-44 (`tests/60-jq-construction.sh` sub-case xss) + AC-45 (`tests/61-sql-injection.sh` stub records argv; assert table still exists) |
| EC-21 (emoji 🦀 + RLO ‮ + CJK + surrogate pair) | unicode-emoji-rtl | A.4 (jq `-n --arg` preserves UTF-8 byte-for-byte; no normalization/transliteration) | AC-46 (`tests/62-unicode-roundtrip.sh`; `jq -r ... \| xxd` matches input UTF-8) |
| EC-22 (em dash U+2014 in caller rationale) | n/a (caller data, not writer code) | A.4 (persists verbatim; NFR-5 carve-out bans em dashes only in writer's OWN code/copy) + A.1 (SKILL.md zero em dashes) + A.7 (FR-9 floor includes writer's OWN code grep) | AC-48 (`tests/64-em-dash-caller.sh`; persists byte-equal AND `grep -nP '\x{2014}' record.sh SKILL.md lib/*.sh tests/*.sh` returns zero) |

## FR-9 Floor Assertion (named step + test)

Per task brief requirement 6: rationale never in stdout/stderr/log is a named step + test.

**Implementing step:** A.7 (`.claude/skills/cfn-decisions/lib/help.sh` + FR-9 floor enforcement). The floor is structural: (a) writer stdout is `printf '%s %s\n' "$DEC_ID" "$STATUS"` only (no rationale; ARCH §3 line 228); (b) writer stderr carries field NAMES, exit codes, and TARGET paths only (never field VALUES; OPS §2 OBS-2/OBS-3 stderr shapes); (c) sink subprocess stderr suppressed via `2>/dev/null` (A.6 lib/sink-delegate.sh) so a misbehaving sink cannot leak the rationale; (d) the hook wrapper's `decisions.ledger` log line (Lane B) carries `id=<id> status=<status>` only (regex `^decisions\.ledger id=[^ ]+ status=[^ ]+$`; no rationale substring).

**Test:** AC-15 + AC-39 (`tests/90-no-leak-floor.sh`). Asserts `secret-marker-ZZY-12345` appears ONLY in `planning/.VERIFY_<slug>.decisions.json` and the SQLite `decisions` table; zero matches in `/tmp` (recursive), writer stdout, writer stderr, and the run-log's `decisions.ledger` lines. Floors `secrets_handling` + `pii_if_present`.

## Synthetic Fixtures and Scoped Teardown (per task brief requirement 7)

- **Slugs:** every test fixture slug matches `^test-dec-[a-z0-9]{6}$` (per-test randomization via `mktemp -u` hex suffix); the real `planning/` is NEVER targeted (every test passes `--root "$ROOT"` where `$ROOT` is a per-test `mktemp -d`).
- **Decision ids:** `test-D<NN>` format (e.g. `test-D01`, `test-D1000`); no real planning artifact id is referenced.
- **Rationale leakage marker:** `secret-marker-ZZY-12345` (OPS §2 OBS-5 sentinel); synthetic, not a real secret.
- **SQLite teardown:** `DELETE FROM decisions WHERE slug LIKE 'test-dec-%'` (marker-targeted WHERE clause; never unscoped; never disables FK checks; per CLAUDE.md test-db safety floor).
- **Sink stub teardown:** `rm -rf "$BIN_DIR"` on EXIT trap (per-test PATH prepend dir); the real `decision-log/record.sh` on system PATH is never modified.
- **Production-data tripwire:** no test imports or hardcodes a real user id, project, workspace slug, or decision id. Each fixture id starts with `test-`; if a future edit repoints a fixture at real data, the scoped `WHERE slug LIKE 'test-%'` clause deletes zero production rows (the production slugs do not match the marker).
- **No real planning/ touch:** every test overrides `--root` to a temp dir. The writer's `$(pwd)/planning` default is exercised only by the assembled runtime-observed tests which simulate the coordinator (Lane B step B.5) and even there the `--root` is overridden.

## Commit-Time Documentation (per task brief requirement 8)

NFR-6 requires every commit to update `readme/feature-status.md` and `readme/state-machines.md`. One implementation step is dedicated to this.

| Step | File | Change | Produces | Consumes | Failing test | Verify command | Done predicate |
|---|---|---|---|---|---|---|---|
| D.1 | `readme/feature-status.md` | Add a row for `cfn-decisions` to the features table: `\| cfn-decisions \| 🚧 Beta (new) \| ⚠️ wiring (writer green; hooks land in this PR) \| .claude/skills/cfn-decisions/{record.sh,lib/*,tests/} \| Decisions-ledger writer. Owns the per-run JSON planning/.VERIFY_<slug>.decisions.json; delegates SQLite sync to decision-log/record.sh (D-1 composition). 12-flag CLI + FR-10 defaults + 9-code exit taxonomy (code 6 reserved, D-7). 4 coordinator hook sites with D-8 isolation. 64 ACs (AC-2 upsert cardinality + AC-4 atomic write RED FIRST). Closes gap where section-decisions.sh:38-51 reads the JSON but nothing writes it. \|`. Update the existing `cfn-decide` row (line 483) and `Structured decision store` row (line 495) with a cross-reference note: `Records run-scoped decisions to the per-run ledger (planning/.VERIFY_<slug>.decisions.json) and syncs SQLite via decision-log/record.sh; wired as the canonical recorder at the megaplan L3 decide site.` | `readme/feature-status.md:cfn-decisions-row` | - | (no test; documentation) | `grep -c 'cfn-decisions' readme/feature-status.md` >= 1 | new row present; cross-references to decision-log and section-decisions.sh accurate |
| D.2 | `readme/state-machines.md` | The Decision Record entity is ALREADY documented at lines 172-187 with lifecycle `proposed \| accepted \| superseded`. That existing entry documents the SQLite-sink semantics (where `superseded -> accepted` is ILLEGAL via `--supersede` and requires a new decision_id). The writer introduces a PARALLEL JSON artifact (`.VERIFY_<slug>.decisions.json`) with the SAME three states but EC-16 semantics (all transitions legal both ways; no forward-only gate; no `--supersede` flag surfaced). Per task brief point 8: NO CHANGE to the existing entity documentation (the writer is the recording mechanism, not a new entity). Add ONE sentence to the existing Decision Record section header: "The per-run JSON ledger (`.VERIFY_<slug>.decisions.json`, written by `cfn-decisions/record.sh`) mirrors these states with one relaxation: EC-16 permits any status-to-status transition (including `superseded -> accepted`) to support caller-side mistake correction; the SQLite register enforces the stricter `--supersede` discipline above." | `readme/state-machines.md:decision-record-json-relaxation-note` | - | (no test; documentation) | `grep -c 'cfn-decisions/record.sh' readme/state-machines.md` >= 1 AND `grep -c 'proposed.*accepted.*superseded' readme/state-machines.md` >= 1 (existing entity unchanged) | existing Decision Record section preserved verbatim; new one-sentence note appended documenting the JSON-side EC-16 relaxation; no entity-name change |

Doc steps D.1 + D.2 commit with Lane B (the behavior change lands with the hooks; documentation follows the code, not the other way around).

## Success Criteria

### Quality Gates

- Loop 3 gate score = (AC checks green) / (total AC checks in TEST section 3 + Step 7 promotions) = green_ACs / 66.
- Loop 2 consensus = validators voting PASS / validators spawned.
- Both computed from the VERIFY manifest; never estimated.
- Canonical thresholds per mode: `.claude/skills/cfn-loop-orchestration-v2/THRESHOLDS.md`.

### Deliverables (every line an executable check; Bar A format)

| Deliverable | Check command | Pass condition |
|---|---|---|
| all AC checks green | `bash .claude/skills/cfn-decisions/tests/run-all.sh 2>&1 \| tee "$OUT"` | every row in `planning/TEST_decisions_ledger.md` §3 table green (64 base ACs: AC-1..AC-64) AND 2 Step 7 promotions green (AC-65 timeout wrapper, AC-66 sink-hang behavior; total 66) |
| writer is bash+jq only (NFR-1) | `grep -nE '\bnode\b\|python\|ruby\|node_modules' .claude/skills/cfn-decisions/` returns zero matches | exit 0 from grep, zero lines |
| no Anthropic API calls (NFR-2) | `grep -rE 'anthropic:*\|claude -p\|@anthropic-ai' .claude/skills/cfn-decisions/ .claude/commands/cfn-loop-task.md .claude/skills/cfn-megaplan/SKILL.md` returns zero matches | exit 0, zero lines |
| writer p95 latency (NFR-3) | `bash .claude/skills/cfn-decisions/tests/81-volume-100.sh && bash .claude/skills/cfn-decisions/tests/82-volume-1000.sh 2>&1 \| tee "$OUT"` | p95 < 500ms over 100-row and 1000-row volume (WRITER_LATENCY_P95_MS=500) |
| no em dashes in writer OWN code/copy (NFR-5) | `grep -rnP '\x{2014}' .claude/skills/cfn-decisions/ .claude/commands/cfn-loop-task.md .claude/skills/cfn-megaplan/SKILL.md` returns zero matches | exit 0, zero lines |
| WIRE-1 writer wired at composition root | `bash .claude/skills/cfn-decisions/tests/w-1-writer-wired.sh` | exit 0; `-x` on record.sh, >=50 lines |
| WIRE-2 hooks wired at all 4 sites | `bash .claude/skills/cfn-decisions/tests/w-2-hook-wired.sh` | exit 0; `grep -c 'cfn-decisions/record.sh' .claude/commands/cfn-loop-task.md` >= 3 AND `>= 1` in megaplan/SKILL.md |
| WIRE-3 sink still delegated | `bash .claude/skills/cfn-decisions/tests/w-3-sink-wired.sh` | exit 0; writer invokes `decision-log/record.sh` with documented flags |
| WIRE-4 renderer unchanged (LOCKED) | `bash .claude/skills/cfn-decisions/tests/w-4-renderer-wired.sh` | exit 0; `sha256sum .claude/skills/cfn-workbench/lib/section-decisions.sh \| grep -q '^97ec8ef0f0bccebc8ff0d0024cf85a59b9cc9fe21d23a8aa8f725264417f2fb6'` (hash pinned at plan time per Q3 promotion; drift turns red by design) |
| exit-code taxonomy 0..8 | `bash .claude/skills/cfn-decisions/tests/31-exit-2-cli-parse.sh && bash .claude/skills/cfn-decisions/tests/32-exit-3-jq-build.sh && bash .claude/skills/cfn-decisions/tests/33-exit-4-filesystem.sh && bash .claude/skills/cfn-decisions/tests/34-exit-5-target-corrupt.sh && bash .claude/skills/cfn-decisions/tests/35-exit-6-reserved.sh 2>&1 \| tee "$OUT"` | each exits 0; codes 0/1/7/8 covered by AC-1/AC-3/AC-7/AC-6 respectively |
| SM-1..SM-9 + illegal-table empty | `bash .claude/skills/cfn-decisions/tests/40-sm-transitions.sh 2>&1 \| tee "$OUT"` | exit 0; 9 valid transitions green; static grep `grep -nE 'illegal transition\|cannot transition' .claude/skills/cfn-decisions/record.sh .claude/skills/cfn-decisions/lib/*.sh` returns zero |
| FR-9 no-leak floor | `bash .claude/skills/cfn-decisions/tests/90-no-leak-floor.sh 2>&1 \| tee "$OUT"` | exit 0; `secret-marker-ZZY-12345` appears ONLY in JSON + SQLite |
| hostile-input ECs | `bash .claude/skills/cfn-decisions/tests/60-jq-construction.sh && bash .claude/skills/cfn-decisions/tests/61-sql-injection.sh && bash .claude/skills/cfn-decisions/tests/62-unicode-roundtrip.sh && bash .claude/skills/cfn-decisions/tests/63-oversized-10k.sh && bash .claude/skills/cfn-decisions/tests/64-em-dash-caller.sh 2>&1 \| tee "$OUT"` | all 5 green; EC-13/14/21/22 + EC-4 covered |
| D-8 isolation at all 4 sites | `bash .claude/skills/cfn-decisions/tests/74-hook-isolation-d8.sh 2>&1 \| tee "$OUT"` | exit 0; OBS-4 line emitted at every site on writer failure; coordinator continues |
| D-9 SITE 4 substitution | `bash .claude/skills/cfn-decisions/tests/73-hook-site-4-megaplan.sh 2>&1 \| tee "$OUT"` | exit 0; POST-EDIT line 215: `grep -c 'cfn-decisions' <(sed -n '215p' .claude/skills/cfn-megaplan/SKILL.md)` >= 1 AND `grep -wc 'cfn-decide' <(sed -n '215p' .claude/skills/cfn-megaplan/SKILL.md)` == 0; BOTH JSON and SQLite row land via writer |
| commit-time docs updated (NFR-6) | `grep -c 'cfn-decisions' readme/feature-status.md` >= 1 AND `grep -c 'cfn-decisions/record.sh' readme/state-machines.md` >= 1 | both >= 1 |

Prose criteria (`review complete`, `>=80% coverage`, `documented`) are invalid; Bar A rejects them. Every deliverable above is an executable command with a binary pass condition.

### Performance Benchmarks

Each benchmark is a Deliverables row above (NFR-3 p95 < 500ms over EC-15 100-row volume and EC-23 1000-row volume). The named constant is `WRITER_LATENCY_P95_MS=500` (OPS §7). The second named constant is `MAX_DECISIONS_PER_RUN=1000` (advisory; the writer does not enforce a hard cap, but the perf test asserts p95 holds at this scale).

## Estimation (beta light per write-plan skill)

- **Effort (Lane A):** M (standard). 6 NEW files (record.sh + 5 lib modules + SKILL.md + state-machine-doc.md) + ~30 test files; OP-W3 upsert and OP-W4 sink-delegate carry the most logic. Driven by ARCH NEW count (4 NEW operations of 8).
- **Effort (Lane B):** S (simple). 4 prose+snippet insertions in 2 markdown files; no new code beyond the inline wrapper template (already proven out by assembled tests).
- **Token budget:** implementer spend ~30k (writer libs + tests); validator spend ~20k (4 validators over 64 ACs). No `claude -p` or external-provider calls (NFR-2: zero Anthropic API calls; pure bash+jq). No `--budget=<usd>` cap needed (n/a: no LLM in the loop, OPS §7).
- **Wall-clock critical path:** Lane A (8 steps, sequential per file dependencies) -> Lane B (5 steps; B.1..B.4 could parallelize across files in theory but share the inline wrapper template so serial is safer) -> doc steps D.1 + D.2. Total: ~13 sequential build steps on the critical path.

## Risk Register (beta light)

| id | risk | likelihood | impact | mitigation | owner/trigger |
|----|------|:--:|:--:|-----------|---------------|
| R1 | D-9 SITE 4 substitution breaks an existing megaplan flow (the prior `decision-log/record.sh` direct call had wider argv or side effects the writer does not faithfully mirror) | L | H | AC-12 / `tests/73-hook-site-4-megaplan.sh` asserts BOTH the JSON entry AND the SQLite row land for the same id; WIRE-3 (AC-63) asserts the writer'sink delegation block uses the documented flag set; if the test fails, the writer's argv translation in lib/sink-delegate.sh is missing a flag (add it; do NOT revert D-9) | Implementer: AC-12 red after Lane B step B.4. Escalate to user only if a flag in `decision-log/record.sh:1-12` is undocumented and cannot be inferred. |
| R2 | Concurrent writer invocations (EC-6) produce a duplicate id entry or corrupt JSON under real coordinator parallelism | M | M | AC-49 (`tests/41-concurrency-race.sh`) launches two writers for the same id; POSIX `rename(2)` atomic mv guarantees last-writer-wins with no partial; per-writer mktemp + per-writer EXIT trap guarantee no temp files linger; the SQLite sink's `UNIQUE(project,slug,decision_id) DO UPDATE` mirrors the same semantics so JSON and SQLite stay consistent under the race | Implementer: AC-49 red flags a missing trap or non-atomic write. Coordinator bug if two parallel resolutions of the same id happen (EC-6 accepts last-writer-wins because two parallel resolutions is itself a bug). |
| R3 | The sink's own stderr leaks rationale text (FR-9 violation via the sink subprocess) | M | H | A.6 (lib/sink-delegate.sh) suppresses sink stderr via `2>/dev/null` on the subprocess; the writer cannot guarantee `decision-log/record.sh` honors FR-9 (it is LOCKED and may print argv on error); AC-15 + AC-39 (`tests/90-no-leak-floor.sh`) grep `/tmp`, stdout, stderr, and run-log for the `secret-marker-ZZY-12345` marker; if the marker reaches writer stderr, the `2>/dev/null` is missing or the sink is emitting to a channel the writer does not control (escalate to decision-log skill owner) | Implementer: AC-15 red. Escalate only if the leakage is via a channel the writer cannot suppress. |
| R4 | The LOCKED renderer (`section-decisions.sh:38-51`) evolves its jq projection in a way that requires a field the writer does not emit | L | M | AC-52 (`tests/70-renderer-contract.sh`) runs the literal renderer jq TSV projection over writer output; if the renderer adds a required key, this test surfaces the drift (writer-side fix is to add the key to lib/jq-build.sh ENTRY); the test cites the projection verbatim so a renderer change is caught at the next run | Implementer: AC-52 red. Fix by extending ENTRY (one-line change in lib/jq-build.sh); never modify the LOCKED renderer. |
| R5 | A hook-site anchor in cfn-loop-task.md or cfn-megaplan/SKILL.md drifts (line numbers shift; anchor text changes) between this plan's authoring and Lane B execution | M | L | Lane B steps cite anchor TEXT (not line numbers); line refs are advisory (`~:414`, `~:215`); the implementer re-greps for the anchor text (`grep -n 'consult product-owner one at a time'`, `grep -n 'Record every resolved decision'`) before editing; if the anchor is gone, STOP and surface to the user (do not guess where to insert) | Implementer: anchor grep fails. Escalate to user; the hook-site prose may have been rewritten in a separate change. |
| R6 | WIRE-4 hash pinning RESOLVED at plan time (Step 7 promotion Q3): `section-decisions.sh` sha256 pinned to `97ec8ef0f0bccebc8ff0d0024cf85a59b9cc9fe21d23a8aa8f725264417f2fb6`. Risk shifts from "cannot evaluate" to "drift turns red by design". | L | L | Resolved: AC-64 now asserts `sha256sum .claude/skills/cfn-workbench/lib/section-decisions.sh \| cut -d' ' -f1` == pinned hash. A future legitimate edit to `section-decisions.sh` turns WIRE-4 red by design (drift signal); the implementer re-pins the hash in AC-64 + PLAN WIRE-4 row only if the renderer change is intentional and re-approved (Bar A re-bless + Bar B re-run per Step 7 re-gating table). | Implementer: WIRE-4 red after a renderer edit. Re-pin the hash in AC-64 + PLAN only if the change is intentional; otherwise investigate unplanned drift. |
| R7 | Em dashes (U+2014) leak into the writer's OWN code/comments/SKILL.md via copy-paste from this plan or from PSEUDO/ARCH artifacts (which use em dashes in prose) | M | L | A.1 (SKILL.md) + A.7 (FR-9 floor source-grep) enforce NFR-5; AC-48 source-grep sub-assertion `grep -rnP '\x{2014}' .claude/skills/cfn-decisions/` returns zero; the writer's own prose is scrubbed at commit time; caller-supplied rationale text is NOT in scope (EC-22 carve-out) | Implementer: AC-48 red on the source-grep half. Replace em dashes with periods, commas, colons, or parentheses per CLAUDE.md Content Standards. |

## Iteration Strategy

- Max iterations: 10 (beta per write-plan skill)
- Confidence threshold: per `.claude/skills/cfn-loop-orchestration-v2/THRESHOLDS.md` (confidence_gate)
- Adaptive agent spawning: YES
- 3-strike rule: if 3 hypotheses fail during debugging (e.g. AC-2 cardinality keeps appending, AC-4 atomic keeps showing partial files, AC-15 leak keeps surfacing the marker), STOP and escalate per CLAUDE.md Debugging Protocol.

## Build Discipline (standing instruction to every Loop 3 implementer)

Climb the cfn-arch build ladder before writing anything: YAGNI -> reuse in-codebase -> stdlib -> native platform -> reuse installed dep -> one line -> minimum new code -> (last resort) add a NEW dependency.

- **A NEW dependency is a planned line item, never an implementer's silent choice.** This plan introduces ZERO new dependencies (bash + jq only per NFR-1; both are pre-existing in the renderer and bless-verify.sh baselines).
- **Security carve-out:** never hand-roll crypto/auth/token parsing/sanitization to avoid a dep. This plan does no auth/crypto; jq `--arg` is the JSON-escape authority (FR-6); the sink parameterizes SQL (LOCKED).
- **Reuse commitments** (DRY, ARCH §0 build-ladder dispositions):
  - OP-W1b refuse_on_missing_or_invalid: pattern reused verbatim from `.claude/skills/cfn-megaplan/bars/bless-verify.sh:80-94` (first-missing-field rule). Re-implemented in the writer because cross-skill bash helpers are not imported (skills CLAUDE.md Minimal Dependencies).
  - OP-W2 build_decision_object: jq `-n --arg/--argjson` escape pattern reused from `bless-verify.sh:137-141`.
  - OP-W3 upsert_by_key_atomic: DIR/BASE/mktemp+mv atomic-write pattern reused from `bless-verify.sh:62-66,144-148`. EXTENSION: upsert-by-key (replace by `.id`) instead of append-only. Justified by SPEC FR-2.
  - OP-W4 delegate_to_record_sh: REUSE subprocess composition (D-1); the writer NEVER opens `decisions.db` directly and NEVER duplicates the sink's SQL.

## Next Steps

1. Review this plan.
2. (Optional) Run `/cfn-plan-review` for dependency trace + blast radius + gap analysis (recommended for the D-9 SITE 4 substitution).
3. Execute CFN Loop (subscription-backed):
   ```bash
   /cfn-loop-task "Build the decisions-ledger writer that closes the gap where cfn-workbench's renderer reads planning/.VERIFY_<slug>.decisions.json (section-decisions.sh:38-51) but nothing writes it" --mode=beta
   ```
4. The plan executes Lane A (writer skill, 8 steps) first; Lane B (coordinator hooks, 5 steps) starts ONLY after `bash .claude/skills/cfn-decisions/tests/run-all.sh` exits 0. Doc steps D.1 + D.2 commit with Lane B.
5. After the loop exits, OPS §3 Stage 3 (end-to-end verify) is the canary: a real loop-task run reaching a Phase 4.2 decision point produces a `.VERIFY_<slug>.decisions.json` entry whose id matches a resolved decision AND the run log contains the matching OBS-1 line AND `JSON_COUNT == LOG_COUNT` per the OPS §2.4 on-call query.
