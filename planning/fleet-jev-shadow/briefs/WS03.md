# WS03 brief: SKILL.md shadow wiring

Run dir: `planning/fleet-jev-shadow/`. Repo root: `/home/masha/projects/claude-flow-novice`. You read AGENTS.md, not CLAUDE.md: this brief is self-contained, follow it exactly.

## Task

Execute **Phase 3** of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first. Your edit target is `.claude/skills/cfn-vote-implement/SKILL.md`.

## Claims (only these files)

- `tests/test-jev-shadow-wiring.sh` (new)
- `.claude/skills/cfn-vote-implement/SKILL.md` (edit)

## Method (TDD)

1. Write `tests/test-jev-shadow-wiring.sh` first: 5 greps against SKILL.md per plan Phase 3 row 1 (a-e), failure prints `missing=[...] unexpected=[]`. Run it: must be RED.
2. Edit SKILL.md with the 5 insertions per plan Phase 3 row 2 (a-e). Insertion anchors: Voting Protocol numbered list (before "All 3 agents receive the full manifest"), after the tally table (rows "3/3" to "0/3"), after the Manifest Resumability list, the `--dry-run` clause, and one line in the Usage section.
3. Re-run: must be GREEN. Then run `bash tests/test-shell-portability.sh`: must exit 0.

## Constraints

- NO em dashes in any text you write (SKILL.md insertions, test code, comments).
- Invocation paths in SKILL.md text must be `$HOME/.claude/skills/cfn-vote-implement/<script>.sh`, never cwd-relative `.claude/skills/...`.
- Label both shadow steps "SHADOW" with the plan's exact wording: logs only, decides nothing, output must not be shown to voters.
- You may run ONLY: `bash tests/test-jev-shadow-wiring.sh`, `bash tests/test-shell-portability.sh`, `bash -n <your files>`. No other suites.
- For every file: `~/.claude/hooks/cfn-invoke-pre-edit.sh <file> --agent-id WS03` before, `~/.claude/hooks/cfn-invoke-post-edit.sh <file> --agent-id WS03` after.

## Fleet protocol

- On every edit-set change: `fleet heartbeat WS03 --files <comma list>`.
- Commit: `fleet commit WS03 -m "feat(jev): wire shadow triage steps into vote skill" --force-with-note`.
- Never push. Never edit files outside your claims.
- Done: `fleet heartbeat WS03 done: wiring test green, portability green, committed`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
