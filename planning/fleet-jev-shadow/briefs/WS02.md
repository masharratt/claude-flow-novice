# WS02 brief: Jev triage/record/report scripts

Run dir: `planning/fleet-jev-shadow/` (this directory's parent). Repo root: `/home/masha/projects/claude-flow-novice`.

## Task

Execute **Phase 2** of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`. Read the whole plan first (Context, Design decisions, Phase 2 table, TDD Sequence, Assumptions). If `.claude/cfn-scripts/jev-systemone.sh` does not exist yet, run `fleet heartbeat WS02 --files <none> blocked: waiting for jev-systemone.sh` and wait; do not implement it yourself.

## Claims (only these files)

- `tests/test-jev-vote-triage.sh` (new)
- `.claude/skills/cfn-vote-implement/jev-triage.sh` (new)
- `.claude/skills/cfn-vote-implement/jev-shadow-record.sh` (new)
- `.claude/skills/cfn-vote-implement/jev-shadow-report.sh` (new)

## Method (TDD, in order)

1. Write `tests/test-jev-vote-triage.sh` first; run `bash tests/test-jev-vote-triage.sh`; it must be RED (scripts absent).
2. Implement the 3 scripts per the plan's Phase 2 rows (exact arg names, log line shapes, exit codes).
3. Re-run; must be GREEN.

## Constraints

- `#!/usr/bin/env bash`, `set -euo pipefail`, `bash -n` clean.
- NO em dashes in any code, comment, or output string.
- Stub curl via PATH per `tests/test-cfn-fleet-dashboard.sh:54-73` + body-file extension (plan describes it).
- flock appends per `.claude/cfn-scripts/log-tool-init-failure.sh:113-117`.
- Choices never on stdout for `jev-triage.sh`.
- You may run ONLY: `bash tests/test-jev-vote-triage.sh` and `bash -n <your files>`. No other suites.
- For every file you create: `~/.claude/hooks/cfn-invoke-pre-edit.sh <file> --agent-id WS02` before, `~/.claude/hooks/cfn-invoke-post-edit.sh <file> --agent-id WS02` after.

## Fleet protocol

- On every edit-set change: `fleet heartbeat WS02 --files <comma list>`.
- Commit: `fleet commit WS02 -m "feat(jev): shadow triage, record, report scripts" --force-with-note` (shared checkout; the guard would otherwise refuse on peers' dirt).
- Never push. Never edit files outside your claims.
- Done: `fleet heartbeat WS02 done: all cases green, files committed` with clean tree on your claims.

You are a fleet worker. Do not spawn subagents; do the work yourself.
