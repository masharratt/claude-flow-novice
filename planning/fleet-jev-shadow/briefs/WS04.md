# WS04 brief: CI test list

Run dir: `planning/fleet-jev-shadow/`. Repo root: `/home/masha/projects/claude-flow-novice`. You read AGENTS.md, not CLAUDE.md: this brief is self-contained, follow it exactly.

## Task

Execute **Phase 4 row 2** of the approved plan at `/home/masha/.claude/plans/snoopy-sprouting-wilkinson.md`.

## Claims (only this file)

- `.github/workflows/ci.yml` (edit)

## Method

1. Read `.github/workflows/ci.yml` and find the shell-test list around lines 238-246 (explicit `tests/test-*.sh` entries).
2. Add these 3 entries following the exact formatting of the existing entries:
   - `tests/test-jev-systemone.sh`
   - `tests/test-jev-vote-triage.sh`
   - `tests/test-jev-shadow-wiring.sh`
3. The test files may not exist yet (sibling lanes create them). That is expected; the master lands everything together before any push. Do not create them yourself.
4. Verify: `grep -c 'test-jev' .github/workflows/ci.yml` prints 3.

## Constraints

- NO em dashes in any comment or text you add.
- Change nothing else in the file.
- For the file: `~/.claude/hooks/cfn-invoke-pre-edit.sh .github/workflows/ci.yml --agent-id WS04` before editing and `~/.claude/hooks/cfn-invoke-post-edit.sh .github/workflows/ci.yml --agent-id WS04` after.

## Fleet protocol

- On edit start: `fleet heartbeat WS04 --files .github/workflows/ci.yml`.
- Commit: `fleet commit WS04 -m "ci: run jev shadow-mode test scripts" --force-with-note`.
- Never push. Never edit files outside your claims.
- Done: `fleet heartbeat WS04 done: 3 test entries listed and committed`.

You are a fleet worker. Do not spawn subagents; do the work yourself.
