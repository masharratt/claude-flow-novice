# CFN Coordinator V3 - Test Coverage Gaps

**Date:** 2025-11-13
**Context:** Coordinator team built dynamic planning + Docker-in-Docker spawning
**Current Coverage:** 53 test cases (P0-P1 focus)
**Missing Coverage:** 8 critical coordinator v3 features

---

## Executive Summary

The coordinator team added:
1. **Dynamic Planning** - Anthropic API calls inside container
2. **Atomic Task Decomposition** - 15-30 min task scopes
3. **Docker-in-Docker Spawning** - Workers spawned via /var/run/docker.sock
4. **Plan File Generation** - /tmp/cfn-docker-plan-*.json

**Current test suite does NOT validate these features.**

---

## Missing Test Coverage

### 1. Dynamic Planning via Anthropic API

**Feature:** `orchestrate.sh` calls Anthropic API to decompose tasks
**Location:** `.claude/skills/cfn-docker-loop-orchestration/orchestrate.sh:252-393`
**Coverage:** 0%

**Tests Needed:**

#### Test 1: Planning Phase Execution
```bash
test_planning_phase_calls_api() {
  # GIVEN coordinator with ANTHROPIC_API_KEY
  # WHEN task description provided
  # THEN plan_task() calls API and generates plan file

  # Validate:
  # - curl command executed with correct endpoint
  # - API key passed in headers
  # - Response parsed into JSON
  # - Plan file created at /tmp/cfn-docker-plan-*.json
}
```

#### Test 2: Plan File Structure Validation
```bash
test_plan_file_has_atomic_tasks() {
  # GIVEN plan file generated by planning phase
  # THEN file contains atomic_tasks array with:
  #   - id, description, estimated_time fields
  #   - dependencies array
  #   - agent_type assignment
  #   - deliverables list
}
```

#### Test 3: Atomic Task Time Bounds
```bash
test_atomic_tasks_within_time_bounds() {
  # GIVEN complex 4-hour task
  # WHEN decomposed by planning phase
  # THEN all atomic tasks have estimated_time between 15-30 min
}
```

**Priority:** P0 (Critical - Core coordinator feature)
**Effort:** 3 hours

---

### 2. Docker-in-Docker Worker Spawning

**Feature:** Coordinator spawns worker containers via Docker socket
**Location:** `.claude/skills/cfn-docker-loop-orchestration/spawn-agent.sh`
**Coverage:** 0%

**Tests Needed:**

#### Test 4: Docker Socket Access Validation
```bash
test_coordinator_has_docker_access() {
  # GIVEN coordinator container running
  # WHEN /var/run/docker.sock mounted
  # THEN docker ps command succeeds inside coordinator
}
```

#### Test 5: Worker Container Spawning
```bash
test_coordinator_spawns_worker_containers() {
  # GIVEN atomic task assigned to backend-developer
  # WHEN spawn_loop3() executes
  # THEN docker run creates worker container with:
  #   - Correct agent image (cfn-agent-backend-developer:latest)
  #   - Task context mounted at /tmp/task-context-*.json
  #   - Network: mcp-network
  #   - Environment: CFN_REDIS_HOST, TASK_ID, etc.
}
```

#### Test 6: Worker Lifecycle Management
```bash
test_workers_execute_and_cleanup() {
  # GIVEN worker container spawned
  # WHEN atomic task completes
  # THEN worker:
  #   - Reports completion to Redis
  #   - Exits with code 0
  #   - Container is removed (--rm flag)
}
```

**Priority:** P0 (Critical - Docker-in-Docker is core architecture)
**Effort:** 4 hours

---

### 3. Atomic Task Assignment

**Feature:** Each worker receives exactly ONE atomic task from plan
**Location:** `orchestrate.sh:451-509` (spawn_loop3 function)
**Coverage:** 0%

**Tests Needed:**

#### Test 7: One Task Per Agent
```bash
test_each_agent_receives_single_atomic_task() {
  # GIVEN plan with 5 atomic tasks
  # WHEN 5 agents spawned
  # THEN each agent context file contains:
  #   - atomic_task: single task description
  #   - expected_deliverables: specific to that task
  #   - estimated_time: 15-30 min
}
```

#### Test 8: Task Context File Format
```bash
test_task_context_matches_specification() {
  # GIVEN worker spawned with context file
  # THEN context file has structure:
  #   {
  #     "task_id": "task-auth-12345",
  #     "loop_number": 3,
  #     "iteration": 1,
  #     "agent_type": "backend-developer",
  #     "atomic_task": "Create JWT service",
  #     "expected_deliverables": "src/auth/jwt.service.ts",
  #     "estimated_time": "25 min"
  #   }
}
```

**Priority:** P1 (High - Task scoping is key differentiator)
**Effort:** 2 hours

---

### 4. Planning Fallback Behavior

**Feature:** Coordinator falls back to keyword matching if no API key
**Location:** `orchestrate.sh:252-393` (else branch)
**Coverage:** 0%

**Tests Needed:**

#### Test 9: Fallback Without API Key
```bash
test_planning_falls_back_without_api_key() {
  # GIVEN coordinator without ANTHROPIC_API_KEY
  # WHEN task description provided
  # THEN coordinator:
  #   - Waits 30 seconds for external plan file
  #   - Falls back to keyword matching
  #   - Generates simple plan based on agent types in description
}
```

**Priority:** P2 (Medium - Edge case but documented behavior)
**Effort:** 1 hour

---

### 5. Plan Validation and Error Handling

**Feature:** Coordinator validates plan before execution
**Coverage:** 0%

**Tests Needed:**

#### Test 10: Invalid Plan Rejection
```bash
test_coordinator_rejects_invalid_plan() {
  # GIVEN plan file with:
  #   - Missing agent_type
  #   - Invalid dependencies (circular)
  #   - estimated_time > 60 min
  # WHEN coordinator reads plan
  # THEN execution aborts with clear error message
}
```

#### Test 11: Dependency Ordering
```bash
test_tasks_execute_in_dependency_order() {
  # GIVEN plan with dependencies:
  #   task-1: no deps
  #   task-2: depends on task-1
  #   task-3: depends on task-2
  # WHEN coordinator executes plan
  # THEN tasks spawn in order: 1 → 2 → 3
}
```

**Priority:** P1 (High - Prevents execution failures)
**Effort:** 3 hours

---

### 6. Coordinator Entrypoint Validation

**Feature:** coordinator-entrypoint.sh validates environment before execution
**Location:** `Dockerfile.cfn-coordinator` (lines 30-160)
**Coverage:** Partial (we tested manually, no automated test)

**Tests Needed:**

#### Test 12: Environment Validation
```bash
test_entrypoint_validates_requirements() {
  # WHEN coordinator starts without required env vars
  # THEN exits with error code 1 and message:
  #   - "TASK_ID environment variable required"
  #   - "TASK_DESCRIPTION environment variable required"
}
```

#### Test 13: Docker Socket Validation
```bash
test_entrypoint_checks_docker_access() {
  # WHEN coordinator starts without docker socket mount
  # THEN exits with: "Cannot access Docker daemon"
}
```

#### Test 14: Redis Connectivity Validation
```bash
test_entrypoint_checks_redis() {
  # WHEN coordinator starts but Redis not available
  # THEN exits with: "Cannot connect to Redis at cfn-redis:6379"
}
```

**Priority:** P1 (High - Prevents silent failures)
**Effort:** 2 hours

---

## Proposed Test Files

### New Test Files Needed

**1. `tests/docker/coordinator-planning-tests.sh` (P0)** ✅ COMPLETED
- Tests 1-3, 9 (dynamic planning)
- 4 test cases
- Status: Implemented 2025-11-13
- Coverage: API calls, plan file structure, time bounds, fallback

**2. `tests/docker/coordinator-docker-in-docker-tests.sh` (P0)** ✅ COMPLETED
- Tests 4-6 (Docker-in-Docker spawning)
- 3 test cases
- Status: Implemented 2025-11-13
- Coverage: Docker socket access, worker spawning, lifecycle management

**3. `tests/docker/coordinator-atomic-task-tests.sh` (P1)** ✅ COMPLETED
- Tests 7-8 (atomic task assignment)
- 2 test cases
- Status: Implemented 2025-11-13
- Coverage: One task per agent, context file format validation

**4. `tests/docker/coordinator-validation-tests.sh` (P1)** ✅ COMPLETED
- Tests 10-14 (validation and error handling)
- 5 test cases
- Status: Implemented 2025-11-13
- Coverage: Invalid plan rejection, dependency ordering, entrypoint validation (env vars, Docker socket, Redis)

### Total New Coverage

**New Tests:** 4 files (4 completed, 0 pending) ✅
**New Test Cases:** 14 cases (14 completed, 0 pending) ✅
**Completed Effort:** 15 hours (ALL tests)
**Remaining Effort:** 0 hours
**Status:** COMPLETE - Full coordinator v3 test coverage achieved

---

## Integration Testing Needed

### End-to-End Coordinator Flow

**Test: Full Dashboard Build Workflow**
```bash
test_coordinator_builds_dashboard_end_to_end() {
  # GIVEN task: "Build monitoring dashboard"
  # WHEN /cfn-docker-native executed
  # THEN:
  #   1. Coordinator starts and validates environment
  #   2. Planning phase decomposes into atomic tasks
  #   3. Workers spawned via Docker-in-Docker
  #   4. Each worker completes atomic task
  #   5. Loop 2 validators run
  #   6. Product owner makes decision
  #   7. Dashboard code delivered
}
```

**Validation Points:**
- Plan file exists and valid
- Workers spawned (docker ps shows containers)
- Redis coordination working (tasks queued/completed)
- Deliverables created (files exist in workspace)

**Priority:** P0 (Critical - Validates entire coordinator v3 flow)
**Effort:** 6 hours

---

## Execution Plan

### Week of 2025-11-18 (P0 Critical Tests)

**Phase 8.1: Planning Tests (4 hours)**
- Create coordinator-planning-tests.sh
- Tests 1-3: API calls, plan file structure, time bounds
- Validate dynamic planning works

**Phase 8.2: Docker-in-Docker Tests (4 hours)**
- Create coordinator-docker-in-docker-tests.sh
- Tests 4-6: Socket access, worker spawning, lifecycle
- Validate container spawning works

**Phase 8.3: Integration Test (6 hours)**
- Create end-to-end dashboard build test
- Validate full coordinator v3 flow
- Document results

**Total: 14 hours**

### Week of 2025-11-25 (P1 High Priority Tests)

**Phase 8.4: Atomic Task Tests (2 hours)**
- Create coordinator-atomic-task-tests.sh
- Tests 7-8: Task assignment, context format

**Phase 8.5: Validation Tests (5 hours)**
- Create coordinator-validation-tests.sh
- Tests 10-14: Error handling, dependencies, entrypoint

**Total: 7 hours**

---

## Success Metrics

**Coverage Target:**
- Coordinator planning: 100% (all 3 phases)
- Docker-in-Docker: 100% (spawn, execute, cleanup)
- Atomic tasks: 100% (assignment, context, time bounds)
- Validation: 80% (core checks, not exhaustive error cases)

**Pass Rate Target:**
- P0 tests: 100% (critical path must work)
- P1 tests: 90% (high priority, allow some infrastructure deps)
- Integration: 80% (complex, may have environmental issues)

**Documentation:**
- Add coordinator v3 patterns to `tests/docker/ARCHITECTURE.md`
- Update `tests/docker/EXECUTION_GUIDE.md` with coordinator tests
- Document atomic task best practices

---

## References

**Coordinator Documentation:**
- `docs/DOCKER_COORDINATOR_FINAL.md` - Architecture overview
- `docs/DOCKER_COORDINATOR_PLANNING.md` - Planning phase details
- `.claude/skills/cfn-docker-loop-orchestration/orchestrate.sh` - Implementation

**Existing Test Patterns:**
- `tests/docker/coordinator-iteration-tests.sh` - Loop pattern examples
- `tests/docker/redis-coordination-tests.sh` - Redis coordination patterns
- `tests/CLAUDE.md` - Test authoring standards

---

## Conclusion

The coordinator v3 architecture adds **14 new test cases** worth of critical functionality that is **currently untested**.

**Immediate Priority:** Create P0 tests (planning + Docker-in-Docker) to validate core coordinator features work as designed.

**Recommended Action:** Execute Phase 8.1-8.3 (14 hours) to validate production readiness before deploying coordinator v3.
