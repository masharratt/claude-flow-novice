User → Main Chat → CLI Mode (/cfn-loop-cli)
                       ↓
                   Coordinator
                       ↓
                   Orchestrator
                       ↓
                   Agents (Docker)
                       ↓
              Coordination Layer (Redis)
                       ↓
              Persistence Layer (PostgreSQL)