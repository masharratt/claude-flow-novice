export default {
  projectId: process.env.TRIGGER_PROJECT_ID || "claude-flow-novice",
  endpoint: process.env.TRIGGER_ENDPOINT || "ws://localhost:8889",
  maxDuration: 600, // 10 minutes in seconds (required)
  runtime: {
    maxDuration: 600, // 10 minutes
  },
  retries: {
    default: {
      maxAttempts: 3,
      minTimeoutInMs: 1000,
      maxTimeoutInMs: 30000,
      factor: 2,
      randomize: true,
    },
  },
};
