version: '3'
services:
  marketing_coordinator:
    image: marketing-coordinator:latest
    environment:
      - API_KEY=${MARKETING_COORDINATOR_API_KEY}
    deploy:
      replicas: 1
    labels:
      - "deployment=hybrid"
    command: >
      sh -c "echo 'Successfully deployed Marketing Coordinator'"
