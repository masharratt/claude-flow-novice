<!-- GENERATED by cfn-wiki sync. Edit knowledge.json or wiki:enrich blocks. -->
<!-- wiki-fp: 879a36a89cebbe0cb41d00169b3605fcfc116f3c75596a6d184337934e26f34b -->

# Fleet run dashboard

**Status:** dev

Implemented and verified against a Playwright browser suite (tests/test-cfn-fleet-dashboard-ui.cjs) covering the glossary legend, inline term tooltips, status tooltips, notes clamp and live updates; run against synthetic rosters, not a real multi-session run.

**Source:**

- `.claude/skills/cfn-fleet/lib/cmd-dashboard.sh`
- `tests/test-cfn-fleet-dashboard-ui.cjs`

<!-- wiki:enrich id=fleet-dashboard fp=879a36a89cebbe0cb41d00169b3605fcfc116f3c75596a6d184337934e26f34b -->
## Purpose

cmd-dashboard.sh renders <run-dir>/dashboard.html straight from roster.tsv (columns parsed by header name), serves the run dir on 127.0.0.1 via python3 http.server, and re-renders on a fingerprint of roster.tsv, dashboard-events.jsonl, fleet.env and glossary.tsv. An optional glossary.tsv (code TAB meaning rows; codes must match ^[A-Za-z0-9][A-Za-z0-9._+-]*$ and contain a digit, so English words cannot become codes) renders a Code key panel in the Activity aside, merging codes that share one meaning into a single row, while the inline JS refetches glossary.tsv each poll and wraps code occurrences in task and note text with a lookaround-bounded regex carrying the meaning as a hover tooltip. Status pills, tiles and filter chips carry plain-English title texts: landed says a commit exists and work may continue, done says the worker reported finished with a clean tree. Long notes clamp to 4 lines and expand on click or Enter. All interpolated strings pass through _fleet_html_escape (bash) or esc() (JS); hostile roster and glossary cells stay literal.

## Execution flow

## Failures and recovery


## Where to make a change


## Limits of this explanation


## Evidence

Reviewed: 2026-09-14

- `.claude/skills/cfn-fleet/lib/cmd-dashboard.sh:48`: _fleet_dash_status_help maps each closed-vocab status to plain-English title text (landed: has at least one commit, not necessarily finished, fresh heartbeat means work continues; done: worker reported finished with a clean tree); the page JS carries a pinned STATUS_HELP twin.
- `.claude/skills/cfn-fleet/lib/cmd-dashboard.sh:67`: glossary.tsv parsing skips blank lines, # comments, a header row whose first cell is code, codes outside ^[A-Za-z0-9][A-Za-z0-9._+-]*$ and digit-less codes (WARN on stderr, never fatal); meanings cap at 160 chars.
- `.claude/skills/cfn-fleet/lib/cmd-dashboard.sh:94`: The Code key panel always emits the #glossary container (hidden when empty) so the live JS has a stable mount when the file appears mid-run; codes sharing one meaning merge into a single CODE / CODE row.
- `.claude/skills/cfn-fleet/lib/cmd-dashboard.sh:993`: termify wraps code occurrences in already-escaped task and note text with span.term title tooltips, matching via (?<![A-Za-z0-9])CODE(?![A-Za-z0-9]) lookarounds (a \b boundary never fires after a code ending in . or -), longest code first; a failed glossary fetch keeps the last good glossary and terms.
- `tests/test-cfn-fleet-dashboard-ui.cjs:33`: Browser suite asserts the legend keeps valid rows only (header, invalid-code and digit-less rows skipped; aliases merged), hostile glossary meanings stay escaped, term tooltips carry meanings in task and notes, the plain word done stays unwrapped, landed pill tooltip warns landed is not finished, notes clamp to 4 lines and toggle on click and Enter, glossary edits apply live, and the no-JS page shows the bash-rendered legend without inline wrapping.
<!-- /wiki:enrich -->

