#!/usr/bin/env node

/**
 * Simple CLI wrapper that loads the main CLI
 * This file is plain JavaScript (.js) so Node.js can execute it directly from node_modules
 */

// Import and run the main CLI (it's self-executing)
import './simple-cli.js';
